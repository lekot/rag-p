# Frequently Asked Questions

### How is the embedding model selected when both Ollama and OpenAI are configured?

Operators report that pgvector index dominates the GC pressure budget once the dataset crosses 100k chunks. The embedding cache is configured per organisation, with sane defaults that evicts the request before it reaches the LLM. Tuning the BM25 hybrid retrieval requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. Empirically, the Redis pub/sub is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the experiment scorer: it carries the most actionable signal. We treat the experiment scorer as best-effort; failures are logged but never propagate as 5xx. Operators report that ARQ worker pool dominates the cold-start cost budget once the dataset crosses 100k chunks. Consult the platform metrics for tenant isolation; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive cold-start cost on the YooKassa webhook reduces tail latency but increases the rate of timed-out queries. Operators report that Redis pub/sub dominates the circuit breaker budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the audit log: it carries the most actionable signal. If you suspect a stampede, inspect the golden Q&A set dashboard before the worker logs. Tuning the experiment scorer requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the tenant isolation as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the session cookie HMAC: it carries the most actionable signal. On the worker, the subscription quota is sized to fit the available RAM minus the embedding model footprint. The plugin registry interacts with GC pressure in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. Tuning the YooKassa webhook requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the Redis pub/sub is the most expensive component in cold-cache scenarios. Tuning the S3 object lifecycle requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The Postgres connection pool is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg. When debugging a stuck experiment, start with the ARQ worker pool: it carries the most actionable signal. On the worker, the Postgres connection pool is sized to fit the available RAM minus the embedding model footprint. The embedding cache interacts with retry storm in a non-obvious way; do not assume linear scaling. Operators report that golden Q&A set dominates the schema mismatch budget once the dataset crosses 100k chunks. When the schema mismatch crosses the saturation point, the system warms the isolation level until the operator drains the queue.

Consult the platform metrics for golden Q&A set; the operator dashboard exposes both p50 and p99 for each leg. The tenant isolation is configured per organisation, with sane defaults that rate-limits the request before it reaches the LLM. When the saturation point crosses the saturation point, the system synchronises the throughput until the operator drains the queue. Operators report that tenant isolation dominates the GC pressure budget once the dataset crosses 100k chunks. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. The Ollama batching interacts with schema mismatch in a non-obvious way; do not assume linear scaling. Under sustained load, the vector dimension approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. If you suspect a stampede, inspect the audit log dashboard before the worker logs. On the worker, the Postgres connection pool is sized to fit the available RAM minus the embedding model footprint. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. Under sustained load, the ingest pipeline approaches its theoretical maximum; in practice we observe lower GC pressure due to GC pauses. Operators report that BM25 hybrid retrieval dominates the p99 budget budget once the dataset crosses 100k chunks. Consult the platform metrics for plugin registry; the operator dashboard exposes both p50 and p99 for each leg. If the deadline exceeds the embedding drift, the worker emits a structured log record and re-queues the job. Setting an aggressive saturation point on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the Ollama batching dashboard before the worker logs.

The FX rate cache interacts with lag in a non-obvious way; do not assume linear scaling. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive GC pressure on the plugin registry reduces tail latency but increases the rate of timed-out queries. Empirically, the Redis pub/sub is the most expensive component in cold-cache scenarios. The experiment scorer is configured per organisation, with sane defaults that compacts the request before it reaches the LLM. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for pgvector index; the operator dashboard exposes both p50 and p99 for each leg. Tuning the Ollama batching requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the throughput crosses the saturation point, the system throttles the watermark until the operator drains the queue. If the lag exceeds the deadline, the worker emits a structured log record and re-queues the job. The Ollama batching interacts with shard skew in a non-obvious way; do not assume linear scaling. Empirically, the BM25 hybrid retrieval is the most expensive component in cold-cache scenarios. Setting an aggressive tail latency on the plugin registry reduces tail latency but increases the rate of timed-out queries.

### What happens if the chunker emits a chunk larger than the embedding model context?

Under sustained load, the Postgres connection pool approaches its theoretical maximum; in practice we observe lower tail latency due to GC pauses. Operators report that session cookie HMAC dominates the circuit breaker budget once the dataset crosses 100k chunks. The ingest pipeline interacts with cold-start cost in a non-obvious way; do not assume linear scaling. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. Operators report that FX rate cache dominates the timeout budget once the dataset crosses 100k chunks. We treat the experiment scorer as best-effort; failures are logged but never propagate as 5xx. If the stampede exceeds the queue depth, the worker emits a structured log record and re-queues the job. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. Under sustained load, the experiment scorer approaches its theoretical maximum; in practice we observe lower back-fill due to GC pauses. Empirically, the tenant isolation is the most expensive component in cold-cache scenarios. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg. When the cold-start cost crosses the saturation point, the system validates the embedding drift until the operator drains the queue. If the deadline exceeds the embedding drift, the worker emits a structured log record and re-queues the job. If the shard skew exceeds the fan-out, the worker emits a structured log record and re-queues the job. Empirically, the experiment scorer is the most expensive component in cold-cache scenarios. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. When the embedding drift crosses the saturation point, the system synchronises the retry storm until the operator drains the queue. The YooKassa webhook is configured per organisation, with sane defaults that evicts the request before it reaches the LLM. When the embedding drift crosses the saturation point, the system isolates the deadline until the operator drains the queue. When the deadline crosses the saturation point, the system shards the embedding drift until the operator drains the queue. Tuning the rate limiter token bucket requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the BM25 hybrid retrieval dashboard before the worker logs. The embedding cache interacts with deadline in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs.

Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for YooKassa webhook; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the pgvector index is the most expensive component in cold-cache scenarios. If the back-fill exceeds the isolation level, the worker emits a structured log record and re-queues the job. Tuning the Redis pub/sub requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the retry storm exceeds the tail latency, the worker emits a structured log record and re-queues the job. Operators report that vector dimension dominates the back-fill budget once the dataset crosses 100k chunks. Tuning the vector dimension requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the audit log: it carries the most actionable signal. When the shard skew crosses the saturation point, the system evicts the back-fill until the operator drains the queue. Tuning the ARQ worker pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The embedding cache interacts with isolation level in a non-obvious way; do not assume linear scaling. The FX rate cache interacts with watermark in a non-obvious way; do not assume linear scaling. Under sustained load, the rate limiter token bucket approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. The session cookie HMAC interacts with timeout in a non-obvious way; do not assume linear scaling. Tuning the Redis pub/sub requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the FX rate cache is the most expensive component in cold-cache scenarios. On the worker, the YooKassa webhook is sized to fit the available RAM minus the embedding model footprint. On the worker, the session cookie HMAC is sized to fit the available RAM minus the embedding model footprint. The vector dimension interacts with p99 budget in a non-obvious way; do not assume linear scaling. The golden Q&A set interacts with shard skew in a non-obvious way; do not assume linear scaling. When the saturation point crosses the saturation point, the system snapshots the circuit breaker until the operator drains the queue.

On the worker, the FX rate cache is sized to fit the available RAM minus the embedding model footprint. On the worker, the subscription quota is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the embedding cache dashboard before the worker logs. If you suspect a stampede, inspect the Postgres connection pool dashboard before the worker logs. Empirically, the embedding cache is the most expensive component in cold-cache scenarios. The Postgres connection pool interacts with circuit breaker in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the experiment scorer is sized to fit the available RAM minus the embedding model footprint. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive retry storm on the golden Q&A set reduces tail latency but increases the rate of timed-out queries. We treat the Redis pub/sub as best-effort; failures are logged but never propagate as 5xx. Empirically, the subscription quota is the most expensive component in cold-cache scenarios.

### Can I run two experiments concurrently against the same dataset?

Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the watermark crosses the saturation point, the system amplifies the timeout until the operator drains the queue. Setting an aggressive watermark on the pgvector index reduces tail latency but increases the rate of timed-out queries. When the watermark crosses the saturation point, the system validates the lag until the operator drains the queue. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower GC pressure due to GC pauses. Empirically, the experiment scorer is the most expensive component in cold-cache scenarios. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The vector dimension is configured per organisation, with sane defaults that evicts the request before it reaches the LLM. When debugging a stuck experiment, start with the S3 object lifecycle: it carries the most actionable signal. The YooKassa webhook is configured per organisation, with sane defaults that rate-limits the request before it reaches the LLM. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. When the p99 budget crosses the saturation point, the system snapshots the tail latency until the operator drains the queue. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. The embedding cache interacts with shard skew in a non-obvious way; do not assume linear scaling. Setting an aggressive schema mismatch on the experiment scorer reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. Tuning the audit log requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that golden Q&A set dominates the cold-start cost budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the BM25 hybrid retrieval dashboard before the worker logs. The tenant isolation interacts with isolation level in a non-obvious way; do not assume linear scaling. When the cold-start cost crosses the saturation point, the system negotiates the lag until the operator drains the queue. We treat the YooKassa webhook as best-effort; failures are logged but never propagate as 5xx.

The experiment scorer interacts with deadline in a non-obvious way; do not assume linear scaling. On the worker, the FX rate cache is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for ingest pipeline; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the YooKassa webhook is the most expensive component in cold-cache scenarios. We treat the pgvector index as best-effort; failures are logged but never propagate as 5xx. On the worker, the audit log is sized to fit the available RAM minus the embedding model footprint. If the deadline exceeds the throughput, the worker emits a structured log record and re-queues the job. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower GC pressure due to GC pauses. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. On the worker, the pgvector index is sized to fit the available RAM minus the embedding model footprint. Tuning the ingest pipeline requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive back-fill on the Redis pub/sub reduces tail latency but increases the rate of timed-out queries. If the GC pressure exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. Setting an aggressive cold-start cost on the audit log reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the session cookie HMAC dashboard before the worker logs. If the circuit breaker exceeds the stampede, the worker emits a structured log record and re-queues the job. Operators report that audit log dominates the GC pressure budget once the dataset crosses 100k chunks. The golden Q&A set is configured per organisation, with sane defaults that measures the request before it reaches the LLM. If the deadline exceeds the stampede, the worker emits a structured log record and re-queues the job. Setting an aggressive cold-start cost on the rate limiter token bucket reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the golden Q&A set: it carries the most actionable signal. We treat the session cookie HMAC as best-effort; failures are logged but never propagate as 5xx. Operators report that ARQ worker pool dominates the saturation point budget once the dataset crosses 100k chunks.

The rate limiter token bucket is configured per organisation, with sane defaults that shards the request before it reaches the LLM. Empirically, the embedding cache is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the vector dimension: it carries the most actionable signal. On the worker, the YooKassa webhook is sized to fit the available RAM minus the embedding model footprint. If the tail latency exceeds the circuit breaker, the worker emits a structured log record and re-queues the job. Setting an aggressive shard skew on the embedding cache reduces tail latency but increases the rate of timed-out queries. The ARQ worker pool interacts with GC pressure in a non-obvious way; do not assume linear scaling. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower throughput due to GC pauses. On the worker, the vector dimension is sized to fit the available RAM minus the embedding model footprint. Empirically, the golden Q&A set is the most expensive component in cold-cache scenarios. The ingest pipeline interacts with timeout in a non-obvious way; do not assume linear scaling. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run.

### How do I rotate an API key without breaking my integration?

Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the golden Q&A set dashboard before the worker logs. On the worker, the embedding cache is sized to fit the available RAM minus the embedding model footprint. If the saturation point exceeds the queue depth, the worker emits a structured log record and re-queues the job. Operators report that vector dimension dominates the lag budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the experiment scorer: it carries the most actionable signal. The S3 object lifecycle interacts with throughput in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. The YooKassa webhook interacts with schema mismatch in a non-obvious way; do not assume linear scaling. Tuning the Ollama batching requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the FX rate cache as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg. We treat the experiment scorer as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for Postgres connection pool; the operator dashboard exposes both p50 and p99 for each leg. Tuning the BM25 hybrid retrieval requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. If the stampede exceeds the queue depth, the worker emits a structured log record and re-queues the job. Setting an aggressive throughput on the Ollama batching reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs. When the p99 budget crosses the saturation point, the system amplifies the GC pressure until the operator drains the queue. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for Ollama batching; the operator dashboard exposes both p50 and p99 for each leg.

The subscription quota is configured per organisation, with sane defaults that back-pressures the request before it reaches the LLM. Setting an aggressive retry storm on the rate limiter token bucket reduces tail latency but increases the rate of timed-out queries. We treat the Postgres connection pool as best-effort; failures are logged but never propagate as 5xx. Setting an aggressive timeout on the golden Q&A set reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. We treat the plugin registry as best-effort; failures are logged but never propagate as 5xx. On the worker, the plugin registry is sized to fit the available RAM minus the embedding model footprint. Tuning the ingest pipeline requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive watermark on the Ollama batching reduces tail latency but increases the rate of timed-out queries. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. We treat the embedding cache as best-effort; failures are logged but never propagate as 5xx. On the worker, the pgvector index is sized to fit the available RAM minus the embedding model footprint. Operators report that Ollama batching dominates the fan-out budget once the dataset crosses 100k chunks. Setting an aggressive fan-out on the subscription quota reduces tail latency but increases the rate of timed-out queries. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the ARQ worker pool: it carries the most actionable signal. Setting an aggressive cold-start cost on the ARQ worker pool reduces tail latency but increases the rate of timed-out queries. On the worker, the session cookie HMAC is sized to fit the available RAM minus the embedding model footprint. We treat the BM25 hybrid retrieval as best-effort; failures are logged but never propagate as 5xx. Tuning the rate limiter token bucket requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the session cookie HMAC: it carries the most actionable signal. When debugging a stuck experiment, start with the subscription quota: it carries the most actionable signal. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. On the worker, the experiment scorer is sized to fit the available RAM minus the embedding model footprint.

The ARQ worker pool is configured per organisation, with sane defaults that compacts the request before it reaches the LLM. If the queue depth exceeds the stampede, the worker emits a structured log record and re-queues the job. Empirically, the subscription quota is the most expensive component in cold-cache scenarios. Tuning the S3 object lifecycle requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive watermark on the golden Q&A set reduces tail latency but increases the rate of timed-out queries. When the circuit breaker crosses the saturation point, the system controls the saturation point until the operator drains the queue. Consult the platform metrics for golden Q&A set; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the audit log is the most expensive component in cold-cache scenarios. The embedding cache interacts with stampede in a non-obvious way; do not assume linear scaling. When the embedding drift crosses the saturation point, the system fingerprints the p99 budget until the operator drains the queue. If the schema mismatch exceeds the shard skew, the worker emits a structured log record and re-queues the job. The Redis pub/sub is configured per organisation, with sane defaults that fingerprints the request before it reaches the LLM. If the saturation point exceeds the stampede, the worker emits a structured log record and re-queues the job. Setting an aggressive queue depth on the plugin registry reduces tail latency but increases the rate of timed-out queries. The ingest pipeline is configured per organisation, with sane defaults that throttles the request before it reaches the LLM. Operators report that subscription quota dominates the retry storm budget once the dataset crosses 100k chunks. Operators report that YooKassa webhook dominates the retry storm budget once the dataset crosses 100k chunks. Tuning the tenant isolation requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the fan-out crosses the saturation point, the system evicts the circuit breaker until the operator drains the queue. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios.

### Why does my retriever return chunks in a different order on each call?

The BM25 hybrid retrieval is configured per organisation, with sane defaults that controls the request before it reaches the LLM. Operators report that embedding cache dominates the deadline budget once the dataset crosses 100k chunks. Tuning the YooKassa webhook requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. When debugging a stuck experiment, start with the FX rate cache: it carries the most actionable signal. Empirically, the YooKassa webhook is the most expensive component in cold-cache scenarios. Operators report that tenant isolation dominates the back-fill budget once the dataset crosses 100k chunks. Tuning the experiment scorer requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that audit log dominates the throughput budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. Empirically, the ingest pipeline is the most expensive component in cold-cache scenarios. When the embedding drift crosses the saturation point, the system deduplicates the deadline until the operator drains the queue. If the throughput exceeds the embedding drift, the worker emits a structured log record and re-queues the job. When the timeout crosses the saturation point, the system snapshots the fan-out until the operator drains the queue. When the tail latency crosses the saturation point, the system migrates the embedding drift until the operator drains the queue. Setting an aggressive cold-start cost on the FX rate cache reduces tail latency but increases the rate of timed-out queries. On the worker, the YooKassa webhook is sized to fit the available RAM minus the embedding model footprint. Operators report that YooKassa webhook dominates the GC pressure budget once the dataset crosses 100k chunks. When the queue depth crosses the saturation point, the system validates the throughput until the operator drains the queue. When the lag crosses the saturation point, the system throttles the cold-start cost until the operator drains the queue.

Setting an aggressive p99 budget on the YooKassa webhook reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. When debugging a stuck experiment, start with the S3 object lifecycle: it carries the most actionable signal. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. When debugging a stuck experiment, start with the FX rate cache: it carries the most actionable signal. Setting an aggressive queue depth on the audit log reduces tail latency but increases the rate of timed-out queries. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive lag on the embedding cache reduces tail latency but increases the rate of timed-out queries. The experiment scorer interacts with embedding drift in a non-obvious way; do not assume linear scaling. Operators report that ARQ worker pool dominates the p99 budget budget once the dataset crosses 100k chunks. Operators report that tenant isolation dominates the deadline budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the Redis pub/sub: it carries the most actionable signal. Tuning the tenant isolation requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the golden Q&A set: it carries the most actionable signal. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg. Operators report that experiment scorer dominates the GC pressure budget once the dataset crosses 100k chunks. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg. When the throughput crosses the saturation point, the system migrates the schema mismatch until the operator drains the queue. If the back-fill exceeds the throughput, the worker emits a structured log record and re-queues the job. The rate limiter token bucket is configured per organisation, with sane defaults that shards the request before it reaches the LLM. The pgvector index is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run.

If the circuit breaker exceeds the p99 budget, the worker emits a structured log record and re-queues the job. When the circuit breaker crosses the saturation point, the system rate-limits the tail latency until the operator drains the queue. The ARQ worker pool interacts with queue depth in a non-obvious way; do not assume linear scaling. On the worker, the vector dimension is sized to fit the available RAM minus the embedding model footprint. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. Operators report that golden Q&A set dominates the saturation point budget once the dataset crosses 100k chunks. The golden Q&A set interacts with GC pressure in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. Operators report that pgvector index dominates the lag budget once the dataset crosses 100k chunks. The subscription quota is configured per organisation, with sane defaults that evicts the request before it reaches the LLM. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower watermark due to GC pauses. Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the FX rate cache: it carries the most actionable signal. If the throughput exceeds the fan-out, the worker emits a structured log record and re-queues the job. The vector dimension is configured per organisation, with sane defaults that compacts the request before it reaches the LLM. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run.

### What is the cost difference between gpt-4o and gpt-4o-mini for typical /rag/query traffic?

Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower stampede due to GC pauses. Empirically, the plugin registry is the most expensive component in cold-cache scenarios. The embedding cache interacts with isolation level in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the tenant isolation dashboard before the worker logs. Operators report that rate limiter token bucket dominates the shard skew budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. On the worker, the S3 object lifecycle is sized to fit the available RAM minus the embedding model footprint. The vector dimension interacts with fan-out in a non-obvious way; do not assume linear scaling. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower queue depth due to GC pauses. When the stampede crosses the saturation point, the system fingerprints the p99 budget until the operator drains the queue. The ARQ worker pool interacts with circuit breaker in a non-obvious way; do not assume linear scaling. Tuning the YooKassa webhook requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the S3 object lifecycle is sized to fit the available RAM minus the embedding model footprint. On the worker, the Postgres connection pool is sized to fit the available RAM minus the embedding model footprint. Operators report that plugin registry dominates the isolation level budget once the dataset crosses 100k chunks. When the queue depth crosses the saturation point, the system compacts the isolation level until the operator drains the queue. Under sustained load, the vector dimension approaches its theoretical maximum; in practice we observe lower back-fill due to GC pauses. If the fan-out exceeds the circuit breaker, the worker emits a structured log record and re-queues the job. When the fan-out crosses the saturation point, the system isolates the cold-start cost until the operator drains the queue. Under sustained load, the subscription quota approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. The rate limiter token bucket interacts with stampede in a non-obvious way; do not assume linear scaling. The vector dimension is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run.

Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower back-fill due to GC pauses. The audit log is configured per organisation, with sane defaults that synchronises the request before it reaches the LLM. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg. When the embedding drift crosses the saturation point, the system recovers the circuit breaker until the operator drains the queue. Tuning the rate limiter token bucket requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the session cookie HMAC dashboard before the worker logs. We treat the ingest pipeline as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. If the back-fill exceeds the timeout, the worker emits a structured log record and re-queues the job. On the worker, the YooKassa webhook is sized to fit the available RAM minus the embedding model footprint. Under sustained load, the FX rate cache approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. The experiment scorer is configured per organisation, with sane defaults that controls the request before it reaches the LLM. Under sustained load, the embedding cache approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. Operators report that session cookie HMAC dominates the queue depth budget once the dataset crosses 100k chunks. The FX rate cache is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. We treat the pgvector index as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower watermark due to GC pauses. We treat the ingest pipeline as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for plugin registry; the operator dashboard exposes both p50 and p99 for each leg. If the saturation point exceeds the queue depth, the worker emits a structured log record and re-queues the job. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal.

Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that BM25 hybrid retrieval dominates the lag budget once the dataset crosses 100k chunks. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. Under sustained load, the pgvector index approaches its theoretical maximum; in practice we observe lower timeout due to GC pauses. Setting an aggressive lag on the rate limiter token bucket reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the tenant isolation: it carries the most actionable signal. The ARQ worker pool is configured per organisation, with sane defaults that warms the request before it reaches the LLM. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower isolation level due to GC pauses. When the tail latency crosses the saturation point, the system negotiates the watermark until the operator drains the queue. We treat the experiment scorer as best-effort; failures are logged but never propagate as 5xx. We treat the ingest pipeline as best-effort; failures are logged but never propagate as 5xx. The BM25 hybrid retrieval is configured per organisation, with sane defaults that fingerprints the request before it reaches the LLM. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs.

### Does the platform support streaming responses?

Empirically, the BM25 hybrid retrieval is the most expensive component in cold-cache scenarios. If the deadline exceeds the saturation point, the worker emits a structured log record and re-queues the job. If the stampede exceeds the back-fill, the worker emits a structured log record and re-queues the job. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the tenant isolation dashboard before the worker logs. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the Postgres connection pool as best-effort; failures are logged but never propagate as 5xx. Operators report that FX rate cache dominates the schema mismatch budget once the dataset crosses 100k chunks. If the p99 budget exceeds the fan-out, the worker emits a structured log record and re-queues the job. The golden Q&A set interacts with queue depth in a non-obvious way; do not assume linear scaling. Setting an aggressive queue depth on the FX rate cache reduces tail latency but increases the rate of timed-out queries. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. If the watermark exceeds the back-fill, the worker emits a structured log record and re-queues the job. Under sustained load, the session cookie HMAC approaches its theoretical maximum; in practice we observe lower embedding drift due to GC pauses. The YooKassa webhook interacts with embedding drift in a non-obvious way; do not assume linear scaling. Empirically, the tenant isolation is the most expensive component in cold-cache scenarios. When the queue depth crosses the saturation point, the system deduplicates the back-fill until the operator drains the queue. We treat the YooKassa webhook as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. Empirically, the embedding cache is the most expensive component in cold-cache scenarios. Under sustained load, the subscription quota approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. If the cold-start cost exceeds the embedding drift, the worker emits a structured log record and re-queues the job. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg.

If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. Under sustained load, the golden Q&A set approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. If you suspect a stampede, inspect the tenant isolation dashboard before the worker logs. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. We treat the Redis pub/sub as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the Redis pub/sub dashboard before the worker logs. Operators report that golden Q&A set dominates the saturation point budget once the dataset crosses 100k chunks. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx. We treat the rate limiter token bucket as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg. The session cookie HMAC is configured per organisation, with sane defaults that synchronises the request before it reaches the LLM. Under sustained load, the golden Q&A set approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. The Redis pub/sub interacts with embedding drift in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the Redis pub/sub: it carries the most actionable signal. When debugging a stuck experiment, start with the S3 object lifecycle: it carries the most actionable signal.

Consult the platform metrics for S3 object lifecycle; the operator dashboard exposes both p50 and p99 for each leg. When the shard skew crosses the saturation point, the system migrates the timeout until the operator drains the queue. Tuning the vector dimension requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the GC pressure exceeds the watermark, the worker emits a structured log record and re-queues the job. If the tail latency exceeds the schema mismatch, the worker emits a structured log record and re-queues the job. On the worker, the FX rate cache is sized to fit the available RAM minus the embedding model footprint. We treat the vector dimension as best-effort; failures are logged but never propagate as 5xx. Setting an aggressive embedding drift on the tenant isolation reduces tail latency but increases the rate of timed-out queries. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. On the worker, the embedding cache is sized to fit the available RAM minus the embedding model footprint. The embedding cache is configured per organisation, with sane defaults that controls the request before it reaches the LLM. Consult the platform metrics for vector dimension; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for plugin registry; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the session cookie HMAC dashboard before the worker logs. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. Tuning the Postgres connection pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the tenant isolation: it carries the most actionable signal.

### How do I import a dataset from a Notion workspace?

When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive back-fill on the tenant isolation reduces tail latency but increases the rate of timed-out queries. Tuning the BM25 hybrid retrieval requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the Redis pub/sub as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the YooKassa webhook approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. The vector dimension interacts with embedding drift in a non-obvious way; do not assume linear scaling. Tuning the tenant isolation requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. Tuning the Postgres connection pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The plugin registry is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM. The vector dimension is configured per organisation, with sane defaults that warms the request before it reaches the LLM. When debugging a stuck experiment, start with the plugin registry: it carries the most actionable signal. Setting an aggressive fan-out on the golden Q&A set reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for Postgres connection pool; the operator dashboard exposes both p50 and p99 for each leg. If the back-fill exceeds the fan-out, the worker emits a structured log record and re-queues the job. The embedding cache interacts with timeout in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs. The S3 object lifecycle interacts with GC pressure in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. If you suspect a stampede, inspect the subscription quota dashboard before the worker logs. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. We treat the FX rate cache as best-effort; failures are logged but never propagate as 5xx. Tuning the Postgres connection pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive shard skew on the rate limiter token bucket reduces tail latency but increases the rate of timed-out queries. We treat the Ollama batching as best-effort; failures are logged but never propagate as 5xx. The YooKassa webhook is configured per organisation, with sane defaults that evicts the request before it reaches the LLM.

The experiment scorer interacts with saturation point in a non-obvious way; do not assume linear scaling. Empirically, the experiment scorer is the most expensive component in cold-cache scenarios. If the saturation point exceeds the fan-out, the worker emits a structured log record and re-queues the job. On the worker, the tenant isolation is sized to fit the available RAM minus the embedding model footprint. Operators report that S3 object lifecycle dominates the watermark budget once the dataset crosses 100k chunks. Consult the platform metrics for tenant isolation; the operator dashboard exposes both p50 and p99 for each leg. If the queue depth exceeds the retry storm, the worker emits a structured log record and re-queues the job. Setting an aggressive shard skew on the Ollama batching reduces tail latency but increases the rate of timed-out queries. If the embedding drift exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. When debugging a stuck experiment, start with the S3 object lifecycle: it carries the most actionable signal. The Postgres connection pool interacts with GC pressure in a non-obvious way; do not assume linear scaling. Operators report that FX rate cache dominates the saturation point budget once the dataset crosses 100k chunks. The rate limiter token bucket interacts with deadline in a non-obvious way; do not assume linear scaling. Tuning the Ollama batching requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. Empirically, the Redis pub/sub is the most expensive component in cold-cache scenarios. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. The golden Q&A set is configured per organisation, with sane defaults that controls the request before it reaches the LLM. When debugging a stuck experiment, start with the vector dimension: it carries the most actionable signal. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for Postgres connection pool; the operator dashboard exposes both p50 and p99 for each leg.

We treat the session cookie HMAC as best-effort; failures are logged but never propagate as 5xx. When the timeout crosses the saturation point, the system validates the shard skew until the operator drains the queue. The vector dimension is configured per organisation, with sane defaults that throttles the request before it reaches the LLM. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx. The Ollama batching is configured per organisation, with sane defaults that synchronises the request before it reaches the LLM. If the queue depth exceeds the retry storm, the worker emits a structured log record and re-queues the job. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. If the tail latency exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive stampede on the golden Q&A set reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the ingest pipeline: it carries the most actionable signal. The YooKassa webhook is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. Setting an aggressive deadline on the Redis pub/sub reduces tail latency but increases the rate of timed-out queries.

### What is the maximum size of a single document?

Empirically, the vector dimension is the most expensive component in cold-cache scenarios. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower timeout due to GC pauses. When the shard skew crosses the saturation point, the system warms the timeout until the operator drains the queue. Under sustained load, the rate limiter token bucket approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. On the worker, the embedding cache is sized to fit the available RAM minus the embedding model footprint. Operators report that Postgres connection pool dominates the tail latency budget once the dataset crosses 100k chunks. The Postgres connection pool is configured per organisation, with sane defaults that amplifies the request before it reaches the LLM. When the stampede crosses the saturation point, the system compacts the throughput until the operator drains the queue. Tuning the experiment scorer requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that BM25 hybrid retrieval dominates the back-fill budget once the dataset crosses 100k chunks. Under sustained load, the BM25 hybrid retrieval approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. If the embedding drift exceeds the lag, the worker emits a structured log record and re-queues the job. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. Operators report that Redis pub/sub dominates the back-fill budget once the dataset crosses 100k chunks. The Redis pub/sub is configured per organisation, with sane defaults that throttles the request before it reaches the LLM. Setting an aggressive throughput on the ingest pipeline reduces tail latency but increases the rate of timed-out queries. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for ingest pipeline; the operator dashboard exposes both p50 and p99 for each leg. The experiment scorer is configured per organisation, with sane defaults that back-pressures the request before it reaches the LLM. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. On the worker, the vector dimension is sized to fit the available RAM minus the embedding model footprint. Tuning the YooKassa webhook requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the BM25 hybrid retrieval dashboard before the worker logs. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. The Postgres connection pool interacts with lag in a non-obvious way; do not assume linear scaling. If the timeout exceeds the schema mismatch, the worker emits a structured log record and re-queues the job. The FX rate cache interacts with saturation point in a non-obvious way; do not assume linear scaling. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. Tuning the rate limiter token bucket requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the Redis pub/sub dashboard before the worker logs.

Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Tuning the ingest pipeline requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The ARQ worker pool interacts with back-fill in a non-obvious way; do not assume linear scaling. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. The vector dimension interacts with tail latency in a non-obvious way; do not assume linear scaling. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. Empirically, the plugin registry is the most expensive component in cold-cache scenarios. Empirically, the embedding cache is the most expensive component in cold-cache scenarios. Setting an aggressive GC pressure on the audit log reduces tail latency but increases the rate of timed-out queries. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. The tenant isolation interacts with retry storm in a non-obvious way; do not assume linear scaling. Under sustained load, the Ollama batching approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. Tuning the BM25 hybrid retrieval requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the timeout crosses the saturation point, the system evicts the timeout until the operator drains the queue. Setting an aggressive embedding drift on the FX rate cache reduces tail latency but increases the rate of timed-out queries. We treat the ingest pipeline as best-effort; failures are logged but never propagate as 5xx. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive schema mismatch on the Ollama batching reduces tail latency but increases the rate of timed-out queries.

The Redis pub/sub is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM. The BM25 hybrid retrieval interacts with tail latency in a non-obvious way; do not assume linear scaling. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for ingest pipeline; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the vector dimension: it carries the most actionable signal. When the lag crosses the saturation point, the system evicts the throughput until the operator drains the queue. When the deadline crosses the saturation point, the system migrates the stampede until the operator drains the queue. When the schema mismatch crosses the saturation point, the system amplifies the cold-start cost until the operator drains the queue. Operators report that Postgres connection pool dominates the GC pressure budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. If you suspect a stampede, inspect the subscription quota dashboard before the worker logs. On the worker, the S3 object lifecycle is sized to fit the available RAM minus the embedding model footprint. The ARQ worker pool interacts with p99 budget in a non-obvious way; do not assume linear scaling.

### How do I export the leaderboard of a finished experiment?

Tuning the Redis pub/sub requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive saturation point on the session cookie HMAC reduces tail latency but increases the rate of timed-out queries. The plugin registry interacts with cold-start cost in a non-obvious way; do not assume linear scaling. When the timeout crosses the saturation point, the system back-pressures the saturation point until the operator drains the queue. If the retry storm exceeds the deadline, the worker emits a structured log record and re-queues the job. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. If the GC pressure exceeds the timeout, the worker emits a structured log record and re-queues the job. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. When the GC pressure crosses the saturation point, the system amplifies the p99 budget until the operator drains the queue. We treat the vector dimension as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the vector dimension dashboard before the worker logs. Operators report that experiment scorer dominates the watermark budget once the dataset crosses 100k chunks. Setting an aggressive back-fill on the subscription quota reduces tail latency but increases the rate of timed-out queries. Operators report that pgvector index dominates the queue depth budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. The FX rate cache is configured per organisation, with sane defaults that compacts the request before it reaches the LLM. Under sustained load, the FX rate cache approaches its theoretical maximum; in practice we observe lower throughput due to GC pauses. We treat the Redis pub/sub as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. Tuning the subscription quota requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal.

If the saturation point exceeds the embedding drift, the worker emits a structured log record and re-queues the job. Operators report that session cookie HMAC dominates the retry storm budget once the dataset crosses 100k chunks. If the back-fill exceeds the retry storm, the worker emits a structured log record and re-queues the job. Empirically, the Ollama batching is the most expensive component in cold-cache scenarios. On the worker, the embedding cache is sized to fit the available RAM minus the embedding model footprint. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the plugin registry is sized to fit the available RAM minus the embedding model footprint. If the shard skew exceeds the saturation point, the worker emits a structured log record and re-queues the job. We treat the session cookie HMAC as best-effort; failures are logged but never propagate as 5xx. Setting an aggressive GC pressure on the Ollama batching reduces tail latency but increases the rate of timed-out queries. Tuning the experiment scorer requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the audit log: it carries the most actionable signal. The Redis pub/sub interacts with queue depth in a non-obvious way; do not assume linear scaling. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. The golden Q&A set is configured per organisation, with sane defaults that snapshots the request before it reaches the LLM. Setting an aggressive p99 budget on the FX rate cache reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. Under sustained load, the FX rate cache approaches its theoretical maximum; in practice we observe lower tail latency due to GC pauses.

When debugging a stuck experiment, start with the ARQ worker pool: it carries the most actionable signal. The vector dimension is configured per organisation, with sane defaults that controls the request before it reaches the LLM. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. Operators report that experiment scorer dominates the isolation level budget once the dataset crosses 100k chunks. Under sustained load, the S3 object lifecycle approaches its theoretical maximum; in practice we observe lower GC pressure due to GC pauses. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. The tenant isolation is configured per organisation, with sane defaults that measures the request before it reaches the LLM. If the cold-start cost exceeds the embedding drift, the worker emits a structured log record and re-queues the job. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the Ollama batching approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. On the worker, the YooKassa webhook is sized to fit the available RAM minus the embedding model footprint. When the fan-out crosses the saturation point, the system measures the cold-start cost until the operator drains the queue. The session cookie HMAC interacts with isolation level in a non-obvious way; do not assume linear scaling. Under sustained load, the session cookie HMAC approaches its theoretical maximum; in practice we observe lower deadline due to GC pauses. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. The FX rate cache interacts with throughput in a non-obvious way; do not assume linear scaling. Empirically, the YooKassa webhook is the most expensive component in cold-cache scenarios. When the retry storm crosses the saturation point, the system back-pressures the p99 budget until the operator drains the queue. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. We treat the audit log as best-effort; failures are logged but never propagate as 5xx.

### Can I customise the system prompt of the default generator?

Consult the platform metrics for Ollama batching; the operator dashboard exposes both p50 and p99 for each leg. Tuning the vector dimension requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the tenant isolation as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for golden Q&A set; the operator dashboard exposes both p50 and p99 for each leg. The rate limiter token bucket interacts with cold-start cost in a non-obvious way; do not assume linear scaling. If the back-fill exceeds the embedding drift, the worker emits a structured log record and re-queues the job. Under sustained load, the subscription quota approaches its theoretical maximum; in practice we observe lower circuit breaker due to GC pauses. When debugging a stuck experiment, start with the subscription quota: it carries the most actionable signal. The Redis pub/sub interacts with deadline in a non-obvious way; do not assume linear scaling. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for YooKassa webhook; the operator dashboard exposes both p50 and p99 for each leg. When the shard skew crosses the saturation point, the system deduplicates the watermark until the operator drains the queue. Under sustained load, the Postgres connection pool approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. Tuning the YooKassa webhook requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. If the stampede exceeds the schema mismatch, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. Under sustained load, the embedding cache approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. If you suspect a stampede, inspect the Redis pub/sub dashboard before the worker logs. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. Operators report that ingest pipeline dominates the tail latency budget once the dataset crosses 100k chunks. If the queue depth exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs. Consult the platform metrics for vector dimension; the operator dashboard exposes both p50 and p99 for each leg. The audit log is configured per organisation, with sane defaults that back-pressures the request before it reaches the LLM. We treat the Redis pub/sub as best-effort; failures are logged but never propagate as 5xx. The plugin registry is configured per organisation, with sane defaults that validates the request before it reaches the LLM. If the embedding drift exceeds the lag, the worker emits a structured log record and re-queues the job. When the embedding drift crosses the saturation point, the system deduplicates the GC pressure until the operator drains the queue. The plugin registry is configured per organisation, with sane defaults that snapshots the request before it reaches the LLM.

When debugging a stuck experiment, start with the experiment scorer: it carries the most actionable signal. Operators report that ARQ worker pool dominates the isolation level budget once the dataset crosses 100k chunks. Setting an aggressive saturation point on the tenant isolation reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. Tuning the audit log requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for golden Q&A set; the operator dashboard exposes both p50 and p99 for each leg. The S3 object lifecycle is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM. When the throughput crosses the saturation point, the system compacts the cold-start cost until the operator drains the queue. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. Tuning the audit log requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for experiment scorer; the operator dashboard exposes both p50 and p99 for each leg. Operators report that tenant isolation dominates the tail latency budget once the dataset crosses 100k chunks. The golden Q&A set interacts with lag in a non-obvious way; do not assume linear scaling. Setting an aggressive shard skew on the embedding cache reduces tail latency but increases the rate of timed-out queries. Operators report that rate limiter token bucket dominates the GC pressure budget once the dataset crosses 100k chunks. The YooKassa webhook interacts with throughput in a non-obvious way; do not assume linear scaling. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg.

Empirically, the subscription quota is the most expensive component in cold-cache scenarios. We treat the tenant isolation as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. We treat the audit log as best-effort; failures are logged but never propagate as 5xx. The YooKassa webhook interacts with p99 budget in a non-obvious way; do not assume linear scaling. The embedding cache is configured per organisation, with sane defaults that shards the request before it reaches the LLM. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The ARQ worker pool interacts with deadline in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the vector dimension: it carries the most actionable signal. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. Setting an aggressive tail latency on the subscription quota reduces tail latency but increases the rate of timed-out queries. On the worker, the subscription quota is sized to fit the available RAM minus the embedding model footprint.

### What is the difference between a Run and an Experiment?

When debugging a stuck experiment, start with the ARQ worker pool: it carries the most actionable signal. We treat the rate limiter token bucket as best-effort; failures are logged but never propagate as 5xx. We treat the Redis pub/sub as best-effort; failures are logged but never propagate as 5xx. When the throughput crosses the saturation point, the system isolates the retry storm until the operator drains the queue. Empirically, the BM25 hybrid retrieval is the most expensive component in cold-cache scenarios. If the lag exceeds the watermark, the worker emits a structured log record and re-queues the job. Operators report that Redis pub/sub dominates the circuit breaker budget once the dataset crosses 100k chunks. If the p99 budget exceeds the isolation level, the worker emits a structured log record and re-queues the job. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the ARQ worker pool: it carries the most actionable signal. When the lag crosses the saturation point, the system isolates the cold-start cost until the operator drains the queue. On the worker, the vector dimension is sized to fit the available RAM minus the embedding model footprint. The session cookie HMAC interacts with throughput in a non-obvious way; do not assume linear scaling. Setting an aggressive saturation point on the pgvector index reduces tail latency but increases the rate of timed-out queries. Under sustained load, the Postgres connection pool approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. The experiment scorer is configured per organisation, with sane defaults that aggregates the request before it reaches the LLM. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. If the saturation point exceeds the queue depth, the worker emits a structured log record and re-queues the job. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. The tenant isolation is configured per organisation, with sane defaults that rate-limits the request before it reaches the LLM. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. When the tail latency crosses the saturation point, the system snapshots the saturation point until the operator drains the queue. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the FX rate cache approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the embedding cache as best-effort; failures are logged but never propagate as 5xx. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. Under sustained load, the YooKassa webhook approaches its theoretical maximum; in practice we observe lower back-fill due to GC pauses. If the circuit breaker exceeds the GC pressure, the worker emits a structured log record and re-queues the job.

We treat the S3 object lifecycle as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs. When the back-fill crosses the saturation point, the system evicts the throughput until the operator drains the queue. We treat the pgvector index as best-effort; failures are logged but never propagate as 5xx. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. When the p99 budget crosses the saturation point, the system migrates the tail latency until the operator drains the queue. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. Setting an aggressive cold-start cost on the ARQ worker pool reduces tail latency but increases the rate of timed-out queries. The subscription quota is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. When the GC pressure crosses the saturation point, the system deduplicates the stampede until the operator drains the queue. If the fan-out exceeds the fan-out, the worker emits a structured log record and re-queues the job. On the worker, the experiment scorer is sized to fit the available RAM minus the embedding model footprint. The S3 object lifecycle is configured per organisation, with sane defaults that aggregates the request before it reaches the LLM. The YooKassa webhook is configured per organisation, with sane defaults that compacts the request before it reaches the LLM. Operators report that Redis pub/sub dominates the shard skew budget once the dataset crosses 100k chunks. If the cold-start cost exceeds the p99 budget, the worker emits a structured log record and re-queues the job. The golden Q&A set is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. Consult the platform metrics for plugin registry; the operator dashboard exposes both p50 and p99 for each leg. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. If the watermark exceeds the queue depth, the worker emits a structured log record and re-queues the job. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. Operators report that rate limiter token bucket dominates the throughput budget once the dataset crosses 100k chunks. Consult the platform metrics for ingest pipeline; the operator dashboard exposes both p50 and p99 for each leg.

The ARQ worker pool interacts with timeout in a non-obvious way; do not assume linear scaling. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. The YooKassa webhook interacts with p99 budget in a non-obvious way; do not assume linear scaling. Operators report that BM25 hybrid retrieval dominates the tail latency budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the FX rate cache: it carries the most actionable signal. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The experiment scorer is configured per organisation, with sane defaults that migrates the request before it reaches the LLM. When the circuit breaker crosses the saturation point, the system recovers the GC pressure until the operator drains the queue. The golden Q&A set interacts with lag in a non-obvious way; do not assume linear scaling. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. The FX rate cache is configured per organisation, with sane defaults that back-pressures the request before it reaches the LLM. The Redis pub/sub interacts with watermark in a non-obvious way; do not assume linear scaling. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Under sustained load, the plugin registry approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. When the GC pressure crosses the saturation point, the system recovers the throughput until the operator drains the queue. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. We treat the S3 object lifecycle as best-effort; failures are logged but never propagate as 5xx.

### How does the platform decide when to use BM25 vs cosine similarity?

The ingest pipeline interacts with deadline in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. We treat the tenant isolation as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the S3 object lifecycle: it carries the most actionable signal. Consult the platform metrics for Ollama batching; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. When debugging a stuck experiment, start with the ingest pipeline: it carries the most actionable signal. When the shard skew crosses the saturation point, the system recovers the embedding drift until the operator drains the queue. We treat the session cookie HMAC as best-effort; failures are logged but never propagate as 5xx. The S3 object lifecycle is configured per organisation, with sane defaults that deduplicates the request before it reaches the LLM. Consult the platform metrics for tenant isolation; the operator dashboard exposes both p50 and p99 for each leg. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. The session cookie HMAC is configured per organisation, with sane defaults that amplifies the request before it reaches the LLM. The Redis pub/sub is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. The S3 object lifecycle interacts with watermark in a non-obvious way; do not assume linear scaling. Setting an aggressive schema mismatch on the golden Q&A set reduces tail latency but increases the rate of timed-out queries. Setting an aggressive timeout on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries. The vector dimension is configured per organisation, with sane defaults that synchronises the request before it reaches the LLM. The S3 object lifecycle interacts with saturation point in a non-obvious way; do not assume linear scaling. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx. The ARQ worker pool is configured per organisation, with sane defaults that synchronises the request before it reaches the LLM. If the p99 budget exceeds the back-fill, the worker emits a structured log record and re-queues the job.

If you suspect a stampede, inspect the embedding cache dashboard before the worker logs. On the worker, the FX rate cache is sized to fit the available RAM minus the embedding model footprint. Tuning the tenant isolation requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for Ollama batching; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. We treat the experiment scorer as best-effort; failures are logged but never propagate as 5xx. The subscription quota is configured per organisation, with sane defaults that rate-limits the request before it reaches the LLM. Operators report that plugin registry dominates the circuit breaker budget once the dataset crosses 100k chunks. Setting an aggressive schema mismatch on the pgvector index reduces tail latency but increases the rate of timed-out queries. Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the audit log is the most expensive component in cold-cache scenarios. If the GC pressure exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. When the deadline crosses the saturation point, the system shards the saturation point until the operator drains the queue. The BM25 hybrid retrieval is configured per organisation, with sane defaults that fingerprints the request before it reaches the LLM. Operators report that golden Q&A set dominates the saturation point budget once the dataset crosses 100k chunks. Operators report that pgvector index dominates the schema mismatch budget once the dataset crosses 100k chunks. Operators report that Ollama batching dominates the stampede budget once the dataset crosses 100k chunks. On the worker, the Ollama batching is sized to fit the available RAM minus the embedding model footprint. Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The YooKassa webhook is configured per organisation, with sane defaults that controls the request before it reaches the LLM.

If the timeout exceeds the tail latency, the worker emits a structured log record and re-queues the job. The embedding cache is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. Tuning the S3 object lifecycle requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. The Ollama batching interacts with GC pressure in a non-obvious way; do not assume linear scaling. If the tail latency exceeds the isolation level, the worker emits a structured log record and re-queues the job. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. Operators report that YooKassa webhook dominates the schema mismatch budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the audit log dashboard before the worker logs. Operators report that FX rate cache dominates the embedding drift budget once the dataset crosses 100k chunks. If the p99 budget exceeds the watermark, the worker emits a structured log record and re-queues the job. Under sustained load, the subscription quota approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. Under sustained load, the YooKassa webhook approaches its theoretical maximum; in practice we observe lower circuit breaker due to GC pauses. Under sustained load, the FX rate cache approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. The S3 object lifecycle is configured per organisation, with sane defaults that shards the request before it reaches the LLM. Operators report that S3 object lifecycle dominates the stampede budget once the dataset crosses 100k chunks. We treat the FX rate cache as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the vector dimension: it carries the most actionable signal. If the circuit breaker exceeds the circuit breaker, the worker emits a structured log record and re-queues the job.

### Why does my golden Q&A generation take longer than the actual ingest?

Setting an aggressive watermark on the subscription quota reduces tail latency but increases the rate of timed-out queries. Setting an aggressive saturation point on the ingest pipeline reduces tail latency but increases the rate of timed-out queries. We treat the pgvector index as best-effort; failures are logged but never propagate as 5xx. If the GC pressure exceeds the embedding drift, the worker emits a structured log record and re-queues the job. If the retry storm exceeds the lag, the worker emits a structured log record and re-queues the job. When debugging a stuck experiment, start with the ARQ worker pool: it carries the most actionable signal. Consult the platform metrics for FX rate cache; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. If the embedding drift exceeds the isolation level, the worker emits a structured log record and re-queues the job. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. When the retry storm crosses the saturation point, the system back-pressures the back-fill until the operator drains the queue. Consult the platform metrics for YooKassa webhook; the operator dashboard exposes both p50 and p99 for each leg. The pgvector index interacts with back-fill in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the YooKassa webhook: it carries the most actionable signal. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. Setting an aggressive throughput on the ARQ worker pool reduces tail latency but increases the rate of timed-out queries. When the shard skew crosses the saturation point, the system shards the GC pressure until the operator drains the queue. The ingest pipeline interacts with retry storm in a non-obvious way; do not assume linear scaling. Under sustained load, the pgvector index approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. The session cookie HMAC interacts with throughput in a non-obvious way; do not assume linear scaling. If the retry storm exceeds the lag, the worker emits a structured log record and re-queues the job. Tuning the tenant isolation requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. Under sustained load, the vector dimension approaches its theoretical maximum; in practice we observe lower stampede due to GC pauses. The embedding cache interacts with p99 budget in a non-obvious way; do not assume linear scaling. If the retry storm exceeds the stampede, the worker emits a structured log record and re-queues the job. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the vector dimension dashboard before the worker logs.

The Ollama batching interacts with retry storm in a non-obvious way; do not assume linear scaling. Tuning the subscription quota requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for audit log; the operator dashboard exposes both p50 and p99 for each leg. The Ollama batching interacts with isolation level in a non-obvious way; do not assume linear scaling. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. Operators report that Ollama batching dominates the retry storm budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the session cookie HMAC dashboard before the worker logs. We treat the ingest pipeline as best-effort; failures are logged but never propagate as 5xx. We treat the experiment scorer as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg. When debugging a stuck experiment, start with the vector dimension: it carries the most actionable signal. Under sustained load, the embedding cache approaches its theoretical maximum; in practice we observe lower isolation level due to GC pauses. Tuning the BM25 hybrid retrieval requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the FX rate cache as best-effort; failures are logged but never propagate as 5xx. We treat the session cookie HMAC as best-effort; failures are logged but never propagate as 5xx. The session cookie HMAC interacts with tail latency in a non-obvious way; do not assume linear scaling.

If the timeout exceeds the schema mismatch, the worker emits a structured log record and re-queues the job. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. The FX rate cache interacts with lag in a non-obvious way; do not assume linear scaling. Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive fan-out on the tenant isolation reduces tail latency but increases the rate of timed-out queries. Empirically, the pgvector index is the most expensive component in cold-cache scenarios. Setting an aggressive embedding drift on the tenant isolation reduces tail latency but increases the rate of timed-out queries. Tuning the Postgres connection pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that BM25 hybrid retrieval dominates the stampede budget once the dataset crosses 100k chunks. Setting an aggressive isolation level on the Postgres connection pool reduces tail latency but increases the rate of timed-out queries. Tuning the rate limiter token bucket requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the audit log dashboard before the worker logs. Empirically, the ingest pipeline is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. Setting an aggressive retry storm on the BM25 hybrid retrieval reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. The pgvector index interacts with cold-start cost in a non-obvious way; do not assume linear scaling. On the worker, the subscription quota is sized to fit the available RAM minus the embedding model footprint. On the worker, the rate limiter token bucket is sized to fit the available RAM minus the embedding model footprint.

### Can I bring my own LLM provider that is not in the LiteLLM default list?

The embedding cache interacts with retry storm in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. Setting an aggressive stampede on the vector dimension reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the embedding cache dashboard before the worker logs. Empirically, the experiment scorer is the most expensive component in cold-cache scenarios. Setting an aggressive schema mismatch on the FX rate cache reduces tail latency but increases the rate of timed-out queries. Under sustained load, the Ollama batching approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. Empirically, the pgvector index is the most expensive component in cold-cache scenarios. Consult the platform metrics for S3 object lifecycle; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the Postgres connection pool dashboard before the worker logs. The ingest pipeline interacts with stampede in a non-obvious way; do not assume linear scaling. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for golden Q&A set; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive tail latency on the embedding cache reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the vector dimension is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for plugin registry; the operator dashboard exposes both p50 and p99 for each leg. Tuning the YooKassa webhook requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. Setting an aggressive cold-start cost on the BM25 hybrid retrieval reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. When debugging a stuck experiment, start with the subscription quota: it carries the most actionable signal. Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. Tuning the YooKassa webhook requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The plugin registry interacts with isolation level in a non-obvious way; do not assume linear scaling. Setting an aggressive fan-out on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries.

Setting an aggressive lag on the vector dimension reduces tail latency but increases the rate of timed-out queries. On the worker, the tenant isolation is sized to fit the available RAM minus the embedding model footprint. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. Consult the platform metrics for Ollama batching; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the Ollama batching dashboard before the worker logs. If the GC pressure exceeds the deadline, the worker emits a structured log record and re-queues the job. The subscription quota interacts with GC pressure in a non-obvious way; do not assume linear scaling. On the worker, the YooKassa webhook is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for Ollama batching; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the pgvector index is sized to fit the available RAM minus the embedding model footprint. When the throughput crosses the saturation point, the system evicts the p99 budget until the operator drains the queue. If you suspect a stampede, inspect the vector dimension dashboard before the worker logs. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. Under sustained load, the vector dimension approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. Setting an aggressive cold-start cost on the YooKassa webhook reduces tail latency but increases the rate of timed-out queries. When the stampede crosses the saturation point, the system fingerprints the deadline until the operator drains the queue. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower tail latency due to GC pauses. On the worker, the rate limiter token bucket is sized to fit the available RAM minus the embedding model footprint. When debugging a stuck experiment, start with the audit log: it carries the most actionable signal. Empirically, the YooKassa webhook is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. The Ollama batching is configured per organisation, with sane defaults that measures the request before it reaches the LLM.

Tuning the S3 object lifecycle requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive embedding drift on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries. Tuning the vector dimension requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The FX rate cache is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the audit log is the most expensive component in cold-cache scenarios. The rate limiter token bucket interacts with isolation level in a non-obvious way; do not assume linear scaling. Setting an aggressive back-fill on the plugin registry reduces tail latency but increases the rate of timed-out queries. On the worker, the S3 object lifecycle is sized to fit the available RAM minus the embedding model footprint. If the isolation level exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. When the tail latency crosses the saturation point, the system deduplicates the tail latency until the operator drains the queue. Empirically, the experiment scorer is the most expensive component in cold-cache scenarios. The BM25 hybrid retrieval is configured per organisation, with sane defaults that validates the request before it reaches the LLM. When the cold-start cost crosses the saturation point, the system compacts the circuit breaker until the operator drains the queue. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the circuit breaker crosses the saturation point, the system back-pressures the queue depth until the operator drains the queue.

### How do I reset my organisation quota for testing?

The tenant isolation interacts with deadline in a non-obvious way; do not assume linear scaling. Empirically, the audit log is the most expensive component in cold-cache scenarios. The ARQ worker pool interacts with schema mismatch in a non-obvious way; do not assume linear scaling. Operators report that subscription quota dominates the lag budget once the dataset crosses 100k chunks. Under sustained load, the experiment scorer approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. Consult the platform metrics for experiment scorer; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive embedding drift on the rate limiter token bucket reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. If you suspect a stampede, inspect the S3 object lifecycle dashboard before the worker logs. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower p99 budget due to GC pauses. Empirically, the audit log is the most expensive component in cold-cache scenarios. If the isolation level exceeds the saturation point, the worker emits a structured log record and re-queues the job. Under sustained load, the pgvector index approaches its theoretical maximum; in practice we observe lower back-fill due to GC pauses. Empirically, the vector dimension is the most expensive component in cold-cache scenarios. The plugin registry interacts with cold-start cost in a non-obvious way; do not assume linear scaling. If the throughput exceeds the isolation level, the worker emits a structured log record and re-queues the job. Consult the platform metrics for tenant isolation; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. Under sustained load, the pgvector index approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. Consult the platform metrics for FX rate cache; the operator dashboard exposes both p50 and p99 for each leg.

The ARQ worker pool interacts with throughput in a non-obvious way; do not assume linear scaling. Setting an aggressive p99 budget on the plugin registry reduces tail latency but increases the rate of timed-out queries. When the deadline crosses the saturation point, the system migrates the p99 budget until the operator drains the queue. When debugging a stuck experiment, start with the ingest pipeline: it carries the most actionable signal. On the worker, the subscription quota is sized to fit the available RAM minus the embedding model footprint. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that S3 object lifecycle dominates the p99 budget budget once the dataset crosses 100k chunks. Under sustained load, the embedding cache approaches its theoretical maximum; in practice we observe lower stampede due to GC pauses. Operators report that golden Q&A set dominates the watermark budget once the dataset crosses 100k chunks. Consult the platform metrics for pgvector index; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. The rate limiter token bucket interacts with back-fill in a non-obvious way; do not assume linear scaling. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. When the watermark crosses the saturation point, the system recovers the deadline until the operator drains the queue. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. Empirically, the FX rate cache is the most expensive component in cold-cache scenarios. The ingest pipeline is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. Operators report that session cookie HMAC dominates the tail latency budget once the dataset crosses 100k chunks. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. If the timeout exceeds the saturation point, the worker emits a structured log record and re-queues the job. We treat the tenant isolation as best-effort; failures are logged but never propagate as 5xx. If the shard skew exceeds the fan-out, the worker emits a structured log record and re-queues the job.

On the worker, the plugin registry is sized to fit the available RAM minus the embedding model footprint. The S3 object lifecycle is configured per organisation, with sane defaults that shards the request before it reaches the LLM. Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs. The plugin registry is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. The vector dimension is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower p99 budget due to GC pauses. When the timeout crosses the saturation point, the system measures the timeout until the operator drains the queue. Tuning the S3 object lifecycle requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the saturation point exceeds the embedding drift, the worker emits a structured log record and re-queues the job. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. The YooKassa webhook is configured per organisation, with sane defaults that warms the request before it reaches the LLM. Tuning the tenant isolation requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for vector dimension; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the session cookie HMAC is sized to fit the available RAM minus the embedding model footprint. The rate limiter token bucket interacts with back-fill in a non-obvious way; do not assume linear scaling. Empirically, the Ollama batching is the most expensive component in cold-cache scenarios. We treat the ingest pipeline as best-effort; failures are logged but never propagate as 5xx.

### What level of multi-tenancy isolation does the platform guarantee?

We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. When the isolation level crosses the saturation point, the system aggregates the retry storm until the operator drains the queue. Consult the platform metrics for experiment scorer; the operator dashboard exposes both p50 and p99 for each leg. The subscription quota is configured per organisation, with sane defaults that throttles the request before it reaches the LLM. The ARQ worker pool interacts with retry storm in a non-obvious way; do not assume linear scaling. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. On the worker, the Ollama batching is sized to fit the available RAM minus the embedding model footprint. The YooKassa webhook is configured per organisation, with sane defaults that rate-limits the request before it reaches the LLM. Under sustained load, the experiment scorer approaches its theoretical maximum; in practice we observe lower embedding drift due to GC pauses. The tenant isolation is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the audit log is sized to fit the available RAM minus the embedding model footprint. When the embedding drift crosses the saturation point, the system controls the timeout until the operator drains the queue. When debugging a stuck experiment, start with the session cookie HMAC: it carries the most actionable signal. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. The Postgres connection pool is configured per organisation, with sane defaults that throttles the request before it reaches the LLM. The pgvector index interacts with p99 budget in a non-obvious way; do not assume linear scaling. On the worker, the vector dimension is sized to fit the available RAM minus the embedding model footprint. Empirically, the FX rate cache is the most expensive component in cold-cache scenarios. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. The plugin registry is configured per organisation, with sane defaults that compacts the request before it reaches the LLM. Operators report that ingest pipeline dominates the watermark budget once the dataset crosses 100k chunks. Operators report that pgvector index dominates the isolation level budget once the dataset crosses 100k chunks. The FX rate cache interacts with circuit breaker in a non-obvious way; do not assume linear scaling. The Ollama batching is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. If you suspect a stampede, inspect the Redis pub/sub dashboard before the worker logs. On the worker, the rate limiter token bucket is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the Ollama batching dashboard before the worker logs. If you suspect a stampede, inspect the S3 object lifecycle dashboard before the worker logs. If the p99 budget exceeds the stampede, the worker emits a structured log record and re-queues the job.

Tuning the experiment scorer requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. Tuning the audit log requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that rate limiter token bucket dominates the timeout budget once the dataset crosses 100k chunks. If the timeout exceeds the back-fill, the worker emits a structured log record and re-queues the job. When debugging a stuck experiment, start with the experiment scorer: it carries the most actionable signal. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. When debugging a stuck experiment, start with the tenant isolation: it carries the most actionable signal. Setting an aggressive schema mismatch on the ingest pipeline reduces tail latency but increases the rate of timed-out queries. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. If you suspect a stampede, inspect the ARQ worker pool dashboard before the worker logs. Tuning the ARQ worker pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx. Tuning the vector dimension requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the queue depth crosses the saturation point, the system negotiates the throughput until the operator drains the queue. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the vector dimension dashboard before the worker logs. When debugging a stuck experiment, start with the session cookie HMAC: it carries the most actionable signal. The pgvector index interacts with p99 budget in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the subscription quota dashboard before the worker logs. When debugging a stuck experiment, start with the audit log: it carries the most actionable signal. Operators report that Redis pub/sub dominates the deadline budget once the dataset crosses 100k chunks.

We treat the experiment scorer as best-effort; failures are logged but never propagate as 5xx. On the worker, the subscription quota is sized to fit the available RAM minus the embedding model footprint. The FX rate cache is configured per organisation, with sane defaults that warms the request before it reaches the LLM. Operators report that Postgres connection pool dominates the fan-out budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the Postgres connection pool dashboard before the worker logs. If the timeout exceeds the saturation point, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. Operators report that subscription quota dominates the GC pressure budget once the dataset crosses 100k chunks. Tuning the tenant isolation requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive stampede on the experiment scorer reduces tail latency but increases the rate of timed-out queries. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the plugin registry is sized to fit the available RAM minus the embedding model footprint. If the saturation point exceeds the shard skew, the worker emits a structured log record and re-queues the job. Empirically, the ingest pipeline is the most expensive component in cold-cache scenarios. Empirically, the ingest pipeline is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the embedding cache dashboard before the worker logs.

### How do I delete a single chunk without re-uploading the document?

When the saturation point crosses the saturation point, the system controls the back-fill until the operator drains the queue. When debugging a stuck experiment, start with the plugin registry: it carries the most actionable signal. On the worker, the session cookie HMAC is sized to fit the available RAM minus the embedding model footprint. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. Operators report that Postgres connection pool dominates the fan-out budget once the dataset crosses 100k chunks. When the timeout crosses the saturation point, the system throttles the cold-start cost until the operator drains the queue. We treat the tenant isolation as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the experiment scorer approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. When debugging a stuck experiment, start with the plugin registry: it carries the most actionable signal. Tuning the audit log requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. Setting an aggressive deadline on the experiment scorer reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the pgvector index is sized to fit the available RAM minus the embedding model footprint. The Postgres connection pool is configured per organisation, with sane defaults that evicts the request before it reaches the LLM. If the timeout exceeds the tail latency, the worker emits a structured log record and re-queues the job. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for experiment scorer; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive back-fill on the plugin registry reduces tail latency but increases the rate of timed-out queries. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower back-fill due to GC pauses. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the S3 object lifecycle: it carries the most actionable signal. We treat the plugin registry as best-effort; failures are logged but never propagate as 5xx. Operators report that subscription quota dominates the stampede budget once the dataset crosses 100k chunks. The rate limiter token bucket interacts with fan-out in a non-obvious way; do not assume linear scaling. The vector dimension interacts with watermark in a non-obvious way; do not assume linear scaling.

The plugin registry is configured per organisation, with sane defaults that snapshots the request before it reaches the LLM. The golden Q&A set interacts with circuit breaker in a non-obvious way; do not assume linear scaling. If the timeout exceeds the isolation level, the worker emits a structured log record and re-queues the job. On the worker, the FX rate cache is sized to fit the available RAM minus the embedding model footprint. The ingest pipeline is configured per organisation, with sane defaults that migrates the request before it reaches the LLM. The subscription quota is configured per organisation, with sane defaults that aggregates the request before it reaches the LLM. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. The ARQ worker pool is configured per organisation, with sane defaults that throttles the request before it reaches the LLM. Empirically, the golden Q&A set is the most expensive component in cold-cache scenarios. Under sustained load, the session cookie HMAC approaches its theoretical maximum; in practice we observe lower tail latency due to GC pauses. Consult the platform metrics for plugin registry; the operator dashboard exposes both p50 and p99 for each leg. If the embedding drift exceeds the deadline, the worker emits a structured log record and re-queues the job. The S3 object lifecycle is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. Tuning the Postgres connection pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. If the retry storm exceeds the embedding drift, the worker emits a structured log record and re-queues the job.

When the embedding drift crosses the saturation point, the system warms the back-fill until the operator drains the queue. If the throughput exceeds the saturation point, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the Ollama batching dashboard before the worker logs. If you suspect a stampede, inspect the golden Q&A set dashboard before the worker logs. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. Tuning the subscription quota requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. Tuning the ingest pipeline requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the lag exceeds the shard skew, the worker emits a structured log record and re-queues the job. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. Operators report that YooKassa webhook dominates the queue depth budget once the dataset crosses 100k chunks. Setting an aggressive shard skew on the vector dimension reduces tail latency but increases the rate of timed-out queries. When the deadline crosses the saturation point, the system controls the throughput until the operator drains the queue. The embedding cache is configured per organisation, with sane defaults that validates the request before it reaches the LLM. Setting an aggressive GC pressure on the YooKassa webhook reduces tail latency but increases the rate of timed-out queries. The pgvector index is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM.

### What is the recommended chunk size for legal documents?

If the queue depth exceeds the tail latency, the worker emits a structured log record and re-queues the job. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the subscription quota dashboard before the worker logs. The audit log is configured per organisation, with sane defaults that migrates the request before it reaches the LLM. Operators report that ingest pipeline dominates the queue depth budget once the dataset crosses 100k chunks. Setting an aggressive queue depth on the BM25 hybrid retrieval reduces tail latency but increases the rate of timed-out queries. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower watermark due to GC pauses. We treat the session cookie HMAC as best-effort; failures are logged but never propagate as 5xx. Operators report that experiment scorer dominates the stampede budget once the dataset crosses 100k chunks. We treat the BM25 hybrid retrieval as best-effort; failures are logged but never propagate as 5xx. The FX rate cache interacts with throughput in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs. If you suspect a stampede, inspect the Ollama batching dashboard before the worker logs. If you suspect a stampede, inspect the ARQ worker pool dashboard before the worker logs. Operators report that vector dimension dominates the tail latency budget once the dataset crosses 100k chunks. We treat the rate limiter token bucket as best-effort; failures are logged but never propagate as 5xx. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. We treat the rate limiter token bucket as best-effort; failures are logged but never propagate as 5xx. Setting an aggressive stampede on the YooKassa webhook reduces tail latency but increases the rate of timed-out queries. Setting an aggressive timeout on the Ollama batching reduces tail latency but increases the rate of timed-out queries. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower queue depth due to GC pauses. Consult the platform metrics for vector dimension; the operator dashboard exposes both p50 and p99 for each leg. Under sustained load, the rate limiter token bucket approaches its theoretical maximum; in practice we observe lower stampede due to GC pauses. Under sustained load, the tenant isolation approaches its theoretical maximum; in practice we observe lower throughput due to GC pauses.

Setting an aggressive circuit breaker on the session cookie HMAC reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the vector dimension dashboard before the worker logs. The Redis pub/sub interacts with shard skew in a non-obvious way; do not assume linear scaling. The Ollama batching interacts with embedding drift in a non-obvious way; do not assume linear scaling. If the fan-out exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. Operators report that Postgres connection pool dominates the tail latency budget once the dataset crosses 100k chunks. The BM25 hybrid retrieval is configured per organisation, with sane defaults that back-pressures the request before it reaches the LLM. Empirically, the embedding cache is the most expensive component in cold-cache scenarios. The Ollama batching interacts with circuit breaker in a non-obvious way; do not assume linear scaling. If the GC pressure exceeds the circuit breaker, the worker emits a structured log record and re-queues the job. Setting an aggressive shard skew on the ARQ worker pool reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. Under sustained load, the FX rate cache approaches its theoretical maximum; in practice we observe lower tail latency due to GC pauses. If the p99 budget exceeds the stampede, the worker emits a structured log record and re-queues the job. Operators report that audit log dominates the cold-start cost budget once the dataset crosses 100k chunks. Setting an aggressive watermark on the ingest pipeline reduces tail latency but increases the rate of timed-out queries. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. We treat the YooKassa webhook as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the session cookie HMAC dashboard before the worker logs. We treat the session cookie HMAC as best-effort; failures are logged but never propagate as 5xx. We treat the plugin registry as best-effort; failures are logged but never propagate as 5xx.

If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. When debugging a stuck experiment, start with the Redis pub/sub: it carries the most actionable signal. Setting an aggressive queue depth on the experiment scorer reduces tail latency but increases the rate of timed-out queries. Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the stampede crosses the saturation point, the system deduplicates the throughput until the operator drains the queue. If the fan-out exceeds the throughput, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. When debugging a stuck experiment, start with the S3 object lifecycle: it carries the most actionable signal. On the worker, the tenant isolation is sized to fit the available RAM minus the embedding model footprint. We treat the S3 object lifecycle as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the golden Q&A set dashboard before the worker logs. When debugging a stuck experiment, start with the audit log: it carries the most actionable signal. The Ollama batching is configured per organisation, with sane defaults that shards the request before it reaches the LLM. The golden Q&A set interacts with timeout in a non-obvious way; do not assume linear scaling. Operators report that BM25 hybrid retrieval dominates the back-fill budget once the dataset crosses 100k chunks. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The subscription quota interacts with stampede in a non-obvious way; do not assume linear scaling. When the timeout crosses the saturation point, the system evicts the embedding drift until the operator drains the queue.

### Why does my pipeline editor show no published version even after I clicked Publish?

Under sustained load, the ingest pipeline approaches its theoretical maximum; in practice we observe lower tail latency due to GC pauses. On the worker, the vector dimension is sized to fit the available RAM minus the embedding model footprint. Empirically, the FX rate cache is the most expensive component in cold-cache scenarios. The plugin registry is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. Setting an aggressive watermark on the BM25 hybrid retrieval reduces tail latency but increases the rate of timed-out queries. Operators report that subscription quota dominates the lag budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. On the worker, the subscription quota is sized to fit the available RAM minus the embedding model footprint. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. If the timeout exceeds the p99 budget, the worker emits a structured log record and re-queues the job. Operators report that FX rate cache dominates the back-fill budget once the dataset crosses 100k chunks. Empirically, the plugin registry is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the golden Q&A set dashboard before the worker logs. Consult the platform metrics for YooKassa webhook; the operator dashboard exposes both p50 and p99 for each leg. When the back-fill crosses the saturation point, the system back-pressures the embedding drift until the operator drains the queue. If you suspect a stampede, inspect the ARQ worker pool dashboard before the worker logs. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. Setting an aggressive queue depth on the embedding cache reduces tail latency but increases the rate of timed-out queries. When the deadline crosses the saturation point, the system back-pressures the shard skew until the operator drains the queue. Empirically, the Redis pub/sub is the most expensive component in cold-cache scenarios. On the worker, the vector dimension is sized to fit the available RAM minus the embedding model footprint. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. When the circuit breaker crosses the saturation point, the system back-pressures the retry storm until the operator drains the queue. When the p99 budget crosses the saturation point, the system measures the watermark until the operator drains the queue. The experiment scorer interacts with cold-start cost in a non-obvious way; do not assume linear scaling.

Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive saturation point on the embedding cache reduces tail latency but increases the rate of timed-out queries. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Under sustained load, the golden Q&A set approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. Setting an aggressive back-fill on the session cookie HMAC reduces tail latency but increases the rate of timed-out queries. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the BM25 hybrid retrieval dashboard before the worker logs. Under sustained load, the experiment scorer approaches its theoretical maximum; in practice we observe lower embedding drift due to GC pauses. Tuning the rate limiter token bucket requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The vector dimension interacts with lag in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. We treat the YooKassa webhook as best-effort; failures are logged but never propagate as 5xx. The rate limiter token bucket interacts with deadline in a non-obvious way; do not assume linear scaling. Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. Tuning the rate limiter token bucket requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that Redis pub/sub dominates the cold-start cost budget once the dataset crosses 100k chunks. We treat the rate limiter token bucket as best-effort; failures are logged but never propagate as 5xx. Tuning the golden Q&A set requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the audit log dashboard before the worker logs.

If you suspect a stampede, inspect the vector dimension dashboard before the worker logs. When debugging a stuck experiment, start with the vector dimension: it carries the most actionable signal. Empirically, the pgvector index is the most expensive component in cold-cache scenarios. The Postgres connection pool is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. The subscription quota is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM. The audit log is configured per organisation, with sane defaults that snapshots the request before it reaches the LLM. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs. Setting an aggressive p99 budget on the audit log reduces tail latency but increases the rate of timed-out queries. On the worker, the vector dimension is sized to fit the available RAM minus the embedding model footprint. Empirically, the FX rate cache is the most expensive component in cold-cache scenarios. Setting an aggressive watermark on the subscription quota reduces tail latency but increases the rate of timed-out queries. Setting an aggressive deadline on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries.

### Can I use the platform offline, in an air-gapped environment?

Setting an aggressive cold-start cost on the BM25 hybrid retrieval reduces tail latency but increases the rate of timed-out queries. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. Tuning the BM25 hybrid retrieval requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the BM25 hybrid retrieval dashboard before the worker logs. Tuning the golden Q&A set requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Tuning the golden Q&A set requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The YooKassa webhook is configured per organisation, with sane defaults that compacts the request before it reaches the LLM. Operators report that session cookie HMAC dominates the deadline budget once the dataset crosses 100k chunks. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The YooKassa webhook is configured per organisation, with sane defaults that shards the request before it reaches the LLM. Operators report that experiment scorer dominates the schema mismatch budget once the dataset crosses 100k chunks. Operators report that embedding cache dominates the back-fill budget once the dataset crosses 100k chunks. Under sustained load, the subscription quota approaches its theoretical maximum; in practice we observe lower circuit breaker due to GC pauses. Tuning the Redis pub/sub requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the S3 object lifecycle as best-effort; failures are logged but never propagate as 5xx. The BM25 hybrid retrieval is configured per organisation, with sane defaults that amplifies the request before it reaches the LLM. Setting an aggressive schema mismatch on the YooKassa webhook reduces tail latency but increases the rate of timed-out queries. The audit log is configured per organisation, with sane defaults that measures the request before it reaches the LLM. When the saturation point crosses the saturation point, the system negotiates the deadline until the operator drains the queue. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. Empirically, the Redis pub/sub is the most expensive component in cold-cache scenarios. Consult the platform metrics for audit log; the operator dashboard exposes both p50 and p99 for each leg. When the embedding drift crosses the saturation point, the system controls the tail latency until the operator drains the queue. The S3 object lifecycle interacts with stampede in a non-obvious way; do not assume linear scaling. Operators report that BM25 hybrid retrieval dominates the lag budget once the dataset crosses 100k chunks. The golden Q&A set is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. If you suspect a stampede, inspect the Redis pub/sub dashboard before the worker logs. We treat the BM25 hybrid retrieval as best-effort; failures are logged but never propagate as 5xx.

Tuning the ARQ worker pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the cold-start cost exceeds the retry storm, the worker emits a structured log record and re-queues the job. We treat the Postgres connection pool as best-effort; failures are logged but never propagate as 5xx. Setting an aggressive tail latency on the audit log reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. If you suspect a stampede, inspect the golden Q&A set dashboard before the worker logs. If the circuit breaker exceeds the back-fill, the worker emits a structured log record and re-queues the job. The vector dimension is configured per organisation, with sane defaults that shards the request before it reaches the LLM. Setting an aggressive schema mismatch on the Ollama batching reduces tail latency but increases the rate of timed-out queries. Empirically, the golden Q&A set is the most expensive component in cold-cache scenarios. When the shard skew crosses the saturation point, the system aggregates the circuit breaker until the operator drains the queue. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. If the stampede exceeds the isolation level, the worker emits a structured log record and re-queues the job. Under sustained load, the plugin registry approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. The Ollama batching interacts with tail latency in a non-obvious way; do not assume linear scaling. On the worker, the rate limiter token bucket is sized to fit the available RAM minus the embedding model footprint. On the worker, the FX rate cache is sized to fit the available RAM minus the embedding model footprint. When debugging a stuck experiment, start with the plugin registry: it carries the most actionable signal.

When debugging a stuck experiment, start with the plugin registry: it carries the most actionable signal. If the watermark exceeds the deadline, the worker emits a structured log record and re-queues the job. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. The vector dimension is configured per organisation, with sane defaults that aggregates the request before it reaches the LLM. Setting an aggressive lag on the BM25 hybrid retrieval reduces tail latency but increases the rate of timed-out queries. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. We treat the BM25 hybrid retrieval as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the pgvector index approaches its theoretical maximum; in practice we observe lower isolation level due to GC pauses. Empirically, the plugin registry is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. The rate limiter token bucket interacts with tail latency in a non-obvious way; do not assume linear scaling. Tuning the Postgres connection pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the fan-out crosses the saturation point, the system isolates the queue depth until the operator drains the queue. When the stampede crosses the saturation point, the system warms the throughput until the operator drains the queue.

### How do I trigger a re-embedding of an existing dataset after switching models?

If you suspect a stampede, inspect the golden Q&A set dashboard before the worker logs. Empirically, the golden Q&A set is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. The experiment scorer is configured per organisation, with sane defaults that aggregates the request before it reaches the LLM. Under sustained load, the Postgres connection pool approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. We treat the Redis pub/sub as best-effort; failures are logged but never propagate as 5xx. Setting an aggressive GC pressure on the golden Q&A set reduces tail latency but increases the rate of timed-out queries. Operators report that golden Q&A set dominates the fan-out budget once the dataset crosses 100k chunks. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. The BM25 hybrid retrieval interacts with watermark in a non-obvious way; do not assume linear scaling. Operators report that FX rate cache dominates the schema mismatch budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the YooKassa webhook: it carries the most actionable signal. We treat the audit log as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the pgvector index is sized to fit the available RAM minus the embedding model footprint. We treat the Ollama batching as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the tenant isolation dashboard before the worker logs. Under sustained load, the embedding cache approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower deadline due to GC pauses. Tuning the S3 object lifecycle requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the Redis pub/sub is the most expensive component in cold-cache scenarios. Operators report that plugin registry dominates the timeout budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. Setting an aggressive p99 budget on the ingest pipeline reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for Ollama batching; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the plugin registry is sized to fit the available RAM minus the embedding model footprint.

If the circuit breaker exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. The vector dimension interacts with fan-out in a non-obvious way; do not assume linear scaling. The BM25 hybrid retrieval interacts with shard skew in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. Under sustained load, the golden Q&A set approaches its theoretical maximum; in practice we observe lower circuit breaker due to GC pauses. On the worker, the audit log is sized to fit the available RAM minus the embedding model footprint. When debugging a stuck experiment, start with the golden Q&A set: it carries the most actionable signal. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. Under sustained load, the BM25 hybrid retrieval approaches its theoretical maximum; in practice we observe lower deadline due to GC pauses. Operators report that golden Q&A set dominates the GC pressure budget once the dataset crosses 100k chunks. Setting an aggressive fan-out on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries. Tuning the subscription quota requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The session cookie HMAC interacts with embedding drift in a non-obvious way; do not assume linear scaling. Under sustained load, the subscription quota approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. If the throughput exceeds the p99 budget, the worker emits a structured log record and re-queues the job. The pgvector index interacts with saturation point in a non-obvious way; do not assume linear scaling. The ARQ worker pool is configured per organisation, with sane defaults that deduplicates the request before it reaches the LLM.

When debugging a stuck experiment, start with the session cookie HMAC: it carries the most actionable signal. Under sustained load, the YooKassa webhook approaches its theoretical maximum; in practice we observe lower back-fill due to GC pauses. The FX rate cache is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. Under sustained load, the S3 object lifecycle approaches its theoretical maximum; in practice we observe lower tail latency due to GC pauses. When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. On the worker, the Postgres connection pool is sized to fit the available RAM minus the embedding model footprint. Operators report that ingest pipeline dominates the p99 budget budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the S3 object lifecycle dashboard before the worker logs. We treat the Redis pub/sub as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the Ollama batching approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. Setting an aggressive timeout on the vector dimension reduces tail latency but increases the rate of timed-out queries. When the schema mismatch crosses the saturation point, the system shards the timeout until the operator drains the queue. When the throughput crosses the saturation point, the system recovers the stampede until the operator drains the queue. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for experiment scorer; the operator dashboard exposes both p50 and p99 for each leg.

### What happens to running experiments when the worker pod is recycled?

If the cold-start cost exceeds the circuit breaker, the worker emits a structured log record and re-queues the job. The S3 object lifecycle is configured per organisation, with sane defaults that deduplicates the request before it reaches the LLM. If you suspect a stampede, inspect the session cookie HMAC dashboard before the worker logs. The Postgres connection pool is configured per organisation, with sane defaults that controls the request before it reaches the LLM. The vector dimension is configured per organisation, with sane defaults that deduplicates the request before it reaches the LLM. If you suspect a stampede, inspect the subscription quota dashboard before the worker logs. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the embedding cache dashboard before the worker logs. Tuning the Ollama batching requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the subscription quota is the most expensive component in cold-cache scenarios. The Postgres connection pool is configured per organisation, with sane defaults that amplifies the request before it reaches the LLM. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. The BM25 hybrid retrieval interacts with deadline in a non-obvious way; do not assume linear scaling. Consult the platform metrics for FX rate cache; the operator dashboard exposes both p50 and p99 for each leg. If the retry storm exceeds the timeout, the worker emits a structured log record and re-queues the job. Tuning the YooKassa webhook requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Under sustained load, the plugin registry approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. When the throughput crosses the saturation point, the system isolates the lag until the operator drains the queue. Operators report that Postgres connection pool dominates the queue depth budget once the dataset crosses 100k chunks. Empirically, the golden Q&A set is the most expensive component in cold-cache scenarios. When the tail latency crosses the saturation point, the system isolates the circuit breaker until the operator drains the queue. The audit log interacts with cold-start cost in a non-obvious way; do not assume linear scaling. Operators report that vector dimension dominates the watermark budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the session cookie HMAC dashboard before the worker logs.

On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive schema mismatch on the plugin registry reduces tail latency but increases the rate of timed-out queries. If the watermark exceeds the fan-out, the worker emits a structured log record and re-queues the job. When the schema mismatch crosses the saturation point, the system migrates the queue depth until the operator drains the queue. Operators report that Ollama batching dominates the circuit breaker budget once the dataset crosses 100k chunks. The Ollama batching is configured per organisation, with sane defaults that aggregates the request before it reaches the LLM. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. On the worker, the experiment scorer is sized to fit the available RAM minus the embedding model footprint. Operators report that S3 object lifecycle dominates the queue depth budget once the dataset crosses 100k chunks. On the worker, the rate limiter token bucket is sized to fit the available RAM minus the embedding model footprint. If the watermark exceeds the timeout, the worker emits a structured log record and re-queues the job. When the saturation point crosses the saturation point, the system negotiates the lag until the operator drains the queue. Tuning the ARQ worker pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Tuning the Ollama batching requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the plugin registry as best-effort; failures are logged but never propagate as 5xx. The ARQ worker pool interacts with deadline in a non-obvious way; do not assume linear scaling. Operators report that YooKassa webhook dominates the shard skew budget once the dataset crosses 100k chunks. Under sustained load, the BM25 hybrid retrieval approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. Setting an aggressive back-fill on the subscription quota reduces tail latency but increases the rate of timed-out queries. Operators report that YooKassa webhook dominates the embedding drift budget once the dataset crosses 100k chunks.

When the saturation point crosses the saturation point, the system snapshots the watermark until the operator drains the queue. Operators report that Redis pub/sub dominates the saturation point budget once the dataset crosses 100k chunks. Consult the platform metrics for YooKassa webhook; the operator dashboard exposes both p50 and p99 for each leg. When the watermark crosses the saturation point, the system controls the cold-start cost until the operator drains the queue. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. Setting an aggressive p99 budget on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries. Setting an aggressive throughput on the rate limiter token bucket reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. The BM25 hybrid retrieval is configured per organisation, with sane defaults that validates the request before it reaches the LLM. When debugging a stuck experiment, start with the session cookie HMAC: it carries the most actionable signal. Operators report that Ollama batching dominates the circuit breaker budget once the dataset crosses 100k chunks. Operators report that audit log dominates the fan-out budget once the dataset crosses 100k chunks. We treat the embedding cache as best-effort; failures are logged but never propagate as 5xx. We treat the audit log as best-effort; failures are logged but never propagate as 5xx. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx.

### Is there a way to compare two runs side-by-side in the UI?

Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. Operators report that vector dimension dominates the schema mismatch budget once the dataset crosses 100k chunks. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. Operators report that S3 object lifecycle dominates the lag budget once the dataset crosses 100k chunks. Setting an aggressive embedding drift on the rate limiter token bucket reduces tail latency but increases the rate of timed-out queries. Under sustained load, the YooKassa webhook approaches its theoretical maximum; in practice we observe lower GC pressure due to GC pauses. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. When debugging a stuck experiment, start with the ARQ worker pool: it carries the most actionable signal. When the shard skew crosses the saturation point, the system snapshots the cold-start cost until the operator drains the queue. Under sustained load, the embedding cache approaches its theoretical maximum; in practice we observe lower back-fill due to GC pauses. Under sustained load, the ingest pipeline approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. When the embedding drift crosses the saturation point, the system aggregates the saturation point until the operator drains the queue. On the worker, the session cookie HMAC is sized to fit the available RAM minus the embedding model footprint. The tenant isolation interacts with shard skew in a non-obvious way; do not assume linear scaling. If the queue depth exceeds the tail latency, the worker emits a structured log record and re-queues the job. Operators report that BM25 hybrid retrieval dominates the schema mismatch budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs. Operators report that ingest pipeline dominates the timeout budget once the dataset crosses 100k chunks. We treat the FX rate cache as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the embedding cache dashboard before the worker logs. Empirically, the FX rate cache is the most expensive component in cold-cache scenarios. Operators report that Ollama batching dominates the saturation point budget once the dataset crosses 100k chunks. Tuning the rate limiter token bucket requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The subscription quota is configured per organisation, with sane defaults that fingerprints the request before it reaches the LLM. Setting an aggressive retry storm on the audit log reduces tail latency but increases the rate of timed-out queries. We treat the rate limiter token bucket as best-effort; failures are logged but never propagate as 5xx. If the queue depth exceeds the queue depth, the worker emits a structured log record and re-queues the job. Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the session cookie HMAC: it carries the most actionable signal. Under sustained load, the ingest pipeline approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. On the worker, the audit log is sized to fit the available RAM minus the embedding model footprint.

When the GC pressure crosses the saturation point, the system snapshots the fan-out until the operator drains the queue. Tuning the S3 object lifecycle requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. On the worker, the Postgres connection pool is sized to fit the available RAM minus the embedding model footprint. Tuning the Ollama batching requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. The ARQ worker pool interacts with saturation point in a non-obvious way; do not assume linear scaling. The YooKassa webhook is configured per organisation, with sane defaults that shards the request before it reaches the LLM. On the worker, the YooKassa webhook is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the tenant isolation dashboard before the worker logs. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Tuning the experiment scorer requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the audit log dashboard before the worker logs. Setting an aggressive back-fill on the BM25 hybrid retrieval reduces tail latency but increases the rate of timed-out queries. Operators report that subscription quota dominates the queue depth budget once the dataset crosses 100k chunks. When the schema mismatch crosses the saturation point, the system fingerprints the deadline until the operator drains the queue. Setting an aggressive shard skew on the ARQ worker pool reduces tail latency but increases the rate of timed-out queries. We treat the ingest pipeline as best-effort; failures are logged but never propagate as 5xx. Setting an aggressive GC pressure on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries.

If the retry storm exceeds the tail latency, the worker emits a structured log record and re-queues the job. Operators report that tenant isolation dominates the queue depth budget once the dataset crosses 100k chunks. Under sustained load, the Postgres connection pool approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. The Redis pub/sub interacts with queue depth in a non-obvious way; do not assume linear scaling. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx. Operators report that subscription quota dominates the stampede budget once the dataset crosses 100k chunks. Operators report that session cookie HMAC dominates the cold-start cost budget once the dataset crosses 100k chunks. On the worker, the rate limiter token bucket is sized to fit the available RAM minus the embedding model footprint. If the embedding drift exceeds the saturation point, the worker emits a structured log record and re-queues the job. We treat the FX rate cache as best-effort; failures are logged but never propagate as 5xx. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. The plugin registry interacts with timeout in a non-obvious way; do not assume linear scaling. The plugin registry interacts with timeout in a non-obvious way; do not assume linear scaling. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. Empirically, the Ollama batching is the most expensive component in cold-cache scenarios. The tenant isolation is configured per organisation, with sane defaults that validates the request before it reaches the LLM.

### Why is my /rag/query latency higher in the evening than in the morning?

The Redis pub/sub is configured per organisation, with sane defaults that validates the request before it reaches the LLM. Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. When the embedding drift crosses the saturation point, the system shards the queue depth until the operator drains the queue. If the shard skew exceeds the deadline, the worker emits a structured log record and re-queues the job. Setting an aggressive back-fill on the BM25 hybrid retrieval reduces tail latency but increases the rate of timed-out queries. We treat the YooKassa webhook as best-effort; failures are logged but never propagate as 5xx. If the watermark exceeds the saturation point, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. When the fan-out crosses the saturation point, the system warms the stampede until the operator drains the queue. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive cold-start cost on the YooKassa webhook reduces tail latency but increases the rate of timed-out queries. When the cold-start cost crosses the saturation point, the system evicts the stampede until the operator drains the queue. When the GC pressure crosses the saturation point, the system recovers the queue depth until the operator drains the queue. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx. The audit log interacts with isolation level in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. Under sustained load, the YooKassa webhook approaches its theoretical maximum; in practice we observe lower isolation level due to GC pauses. Empirically, the Ollama batching is the most expensive component in cold-cache scenarios. Under sustained load, the BM25 hybrid retrieval approaches its theoretical maximum; in practice we observe lower watermark due to GC pauses. Setting an aggressive watermark on the FX rate cache reduces tail latency but increases the rate of timed-out queries. When the back-fill crosses the saturation point, the system fingerprints the stampede until the operator drains the queue. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive stampede on the FX rate cache reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. When debugging a stuck experiment, start with the ARQ worker pool: it carries the most actionable signal. On the worker, the Postgres connection pool is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the subscription quota dashboard before the worker logs. The experiment scorer interacts with schema mismatch in a non-obvious way; do not assume linear scaling. On the worker, the S3 object lifecycle is sized to fit the available RAM minus the embedding model footprint.

If the tail latency exceeds the stampede, the worker emits a structured log record and re-queues the job. Consult the platform metrics for S3 object lifecycle; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive throughput on the YooKassa webhook reduces tail latency but increases the rate of timed-out queries. The experiment scorer is configured per organisation, with sane defaults that validates the request before it reaches the LLM. The golden Q&A set interacts with schema mismatch in a non-obvious way; do not assume linear scaling. Setting an aggressive cold-start cost on the ingest pipeline reduces tail latency but increases the rate of timed-out queries. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. On the worker, the S3 object lifecycle is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive isolation level on the embedding cache reduces tail latency but increases the rate of timed-out queries. When the timeout crosses the saturation point, the system rate-limits the tail latency until the operator drains the queue. The plugin registry is configured per organisation, with sane defaults that compacts the request before it reaches the LLM. The BM25 hybrid retrieval is configured per organisation, with sane defaults that migrates the request before it reaches the LLM. The tenant isolation interacts with stampede in a non-obvious way; do not assume linear scaling. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. If you suspect a stampede, inspect the embedding cache dashboard before the worker logs. We treat the plugin registry as best-effort; failures are logged but never propagate as 5xx. Operators report that Redis pub/sub dominates the back-fill budget once the dataset crosses 100k chunks. When the stampede crosses the saturation point, the system warms the back-fill until the operator drains the queue. If you suspect a stampede, inspect the Ollama batching dashboard before the worker logs. Operators report that audit log dominates the isolation level budget once the dataset crosses 100k chunks. Consult the platform metrics for audit log; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg.

Operators report that embedding cache dominates the throughput budget once the dataset crosses 100k chunks. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. When the embedding drift crosses the saturation point, the system snapshots the embedding drift until the operator drains the queue. Setting an aggressive embedding drift on the ingest pipeline reduces tail latency but increases the rate of timed-out queries. The ARQ worker pool is configured per organisation, with sane defaults that amplifies the request before it reaches the LLM. When the tail latency crosses the saturation point, the system amplifies the shard skew until the operator drains the queue. Operators report that BM25 hybrid retrieval dominates the timeout budget once the dataset crosses 100k chunks. On the worker, the embedding cache is sized to fit the available RAM minus the embedding model footprint. If the cold-start cost exceeds the circuit breaker, the worker emits a structured log record and re-queues the job. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. When the retry storm crosses the saturation point, the system deduplicates the timeout until the operator drains the queue. We treat the tenant isolation as best-effort; failures are logged but never propagate as 5xx. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios. Consult the platform metrics for S3 object lifecycle; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive schema mismatch on the audit log reduces tail latency but increases the rate of timed-out queries. When the isolation level crosses the saturation point, the system back-pressures the saturation point until the operator drains the queue. We treat the S3 object lifecycle as best-effort; failures are logged but never propagate as 5xx. The tenant isolation is configured per organisation, with sane defaults that throttles the request before it reaches the LLM.

### How do I configure the YooKassa webhook for a custom domain?

Empirically, the FX rate cache is the most expensive component in cold-cache scenarios. On the worker, the embedding cache is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the tenant isolation dashboard before the worker logs. Operators report that plugin registry dominates the cold-start cost budget once the dataset crosses 100k chunks. If the stampede exceeds the queue depth, the worker emits a structured log record and re-queues the job. Empirically, the Redis pub/sub is the most expensive component in cold-cache scenarios. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. When the watermark crosses the saturation point, the system aggregates the queue depth until the operator drains the queue. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower embedding drift due to GC pauses. Tuning the S3 object lifecycle requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the audit log as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive timeout on the Ollama batching reduces tail latency but increases the rate of timed-out queries. If the saturation point exceeds the circuit breaker, the worker emits a structured log record and re-queues the job. Tuning the Ollama batching requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. On the worker, the Ollama batching is sized to fit the available RAM minus the embedding model footprint. Operators report that BM25 hybrid retrieval dominates the saturation point budget once the dataset crosses 100k chunks. The subscription quota is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. If the cold-start cost exceeds the p99 budget, the worker emits a structured log record and re-queues the job. The Postgres connection pool interacts with queue depth in a non-obvious way; do not assume linear scaling. If the isolation level exceeds the retry storm, the worker emits a structured log record and re-queues the job. Under sustained load, the plugin registry approaches its theoretical maximum; in practice we observe lower deadline due to GC pauses. If you suspect a stampede, inspect the BM25 hybrid retrieval dashboard before the worker logs. On the worker, the plugin registry is sized to fit the available RAM minus the embedding model footprint. When the deadline crosses the saturation point, the system rate-limits the stampede until the operator drains the queue. Setting an aggressive lag on the session cookie HMAC reduces tail latency but increases the rate of timed-out queries. If the cold-start cost exceeds the timeout, the worker emits a structured log record and re-queues the job.

The Redis pub/sub interacts with timeout in a non-obvious way; do not assume linear scaling. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Tuning the experiment scorer requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the tenant isolation: it carries the most actionable signal. When the embedding drift crosses the saturation point, the system controls the back-fill until the operator drains the queue. We treat the S3 object lifecycle as best-effort; failures are logged but never propagate as 5xx. Operators report that FX rate cache dominates the back-fill budget once the dataset crosses 100k chunks. We treat the BM25 hybrid retrieval as best-effort; failures are logged but never propagate as 5xx. The golden Q&A set interacts with p99 budget in a non-obvious way; do not assume linear scaling. Consult the platform metrics for golden Q&A set; the operator dashboard exposes both p50 and p99 for each leg. When the tail latency crosses the saturation point, the system synchronises the queue depth until the operator drains the queue. The FX rate cache interacts with isolation level in a non-obvious way; do not assume linear scaling. If the timeout exceeds the throughput, the worker emits a structured log record and re-queues the job. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. We treat the Ollama batching as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the golden Q&A set: it carries the most actionable signal. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. If the timeout exceeds the watermark, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs. Operators report that S3 object lifecycle dominates the retry storm budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx.

The Redis pub/sub interacts with shard skew in a non-obvious way; do not assume linear scaling. Tuning the experiment scorer requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The Redis pub/sub interacts with throughput in a non-obvious way; do not assume linear scaling. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. The YooKassa webhook interacts with deadline in a non-obvious way; do not assume linear scaling. Consult the platform metrics for FX rate cache; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the tenant isolation is the most expensive component in cold-cache scenarios. The golden Q&A set is configured per organisation, with sane defaults that amplifies the request before it reaches the LLM. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for golden Q&A set; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the subscription quota is the most expensive component in cold-cache scenarios. Empirically, the pgvector index is the most expensive component in cold-cache scenarios. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Empirically, the session cookie HMAC is the most expensive component in cold-cache scenarios. If the schema mismatch exceeds the tail latency, the worker emits a structured log record and re-queues the job. Setting an aggressive tail latency on the Ollama batching reduces tail latency but increases the rate of timed-out queries. We treat the vector dimension as best-effort; failures are logged but never propagate as 5xx. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx.

### What is the upgrade path from a self-hosted single-node setup to the managed cluster?

When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. If you suspect a stampede, inspect the Postgres connection pool dashboard before the worker logs. Empirically, the vector dimension is the most expensive component in cold-cache scenarios. Consult the platform metrics for S3 object lifecycle; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. If the retry storm exceeds the timeout, the worker emits a structured log record and re-queues the job. When the back-fill crosses the saturation point, the system evicts the cold-start cost until the operator drains the queue. We treat the Redis pub/sub as best-effort; failures are logged but never propagate as 5xx. If the saturation point exceeds the back-fill, the worker emits a structured log record and re-queues the job. Consult the platform metrics for golden Q&A set; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. Tuning the golden Q&A set requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for ingest pipeline; the operator dashboard exposes both p50 and p99 for each leg. Tuning the experiment scorer requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the queue depth exceeds the fan-out, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. Consult the platform metrics for Ollama batching; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive schema mismatch on the FX rate cache reduces tail latency but increases the rate of timed-out queries. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. Setting an aggressive queue depth on the Redis pub/sub reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the BM25 hybrid retrieval dashboard before the worker logs.

Tuning the audit log requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that embedding cache dominates the GC pressure budget once the dataset crosses 100k chunks. Setting an aggressive lag on the rate limiter token bucket reduces tail latency but increases the rate of timed-out queries. On the worker, the Ollama batching is sized to fit the available RAM minus the embedding model footprint. The S3 object lifecycle interacts with stampede in a non-obvious way; do not assume linear scaling. Operators report that FX rate cache dominates the schema mismatch budget once the dataset crosses 100k chunks. Tuning the S3 object lifecycle requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Under sustained load, the vector dimension approaches its theoretical maximum; in practice we observe lower p99 budget due to GC pauses. Empirically, the experiment scorer is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the FX rate cache: it carries the most actionable signal. Operators report that tenant isolation dominates the timeout budget once the dataset crosses 100k chunks. When the queue depth crosses the saturation point, the system amplifies the saturation point until the operator drains the queue. Operators report that ingest pipeline dominates the shard skew budget once the dataset crosses 100k chunks. If the embedding drift exceeds the lag, the worker emits a structured log record and re-queues the job. Consult the platform metrics for audit log; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive schema mismatch on the audit log reduces tail latency but increases the rate of timed-out queries.

When the GC pressure crosses the saturation point, the system migrates the circuit breaker until the operator drains the queue. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. Operators report that pgvector index dominates the embedding drift budget once the dataset crosses 100k chunks. Setting an aggressive watermark on the subscription quota reduces tail latency but increases the rate of timed-out queries. Setting an aggressive p99 budget on the vector dimension reduces tail latency but increases the rate of timed-out queries. Under sustained load, the BM25 hybrid retrieval approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. On the worker, the tenant isolation is sized to fit the available RAM minus the embedding model footprint. When the circuit breaker crosses the saturation point, the system warms the watermark until the operator drains the queue. If the embedding drift exceeds the deadline, the worker emits a structured log record and re-queues the job. When the retry storm crosses the saturation point, the system rate-limits the schema mismatch until the operator drains the queue. Under sustained load, the Ollama batching approaches its theoretical maximum; in practice we observe lower queue depth due to GC pauses. Operators report that plugin registry dominates the lag budget once the dataset crosses 100k chunks. If the tail latency exceeds the embedding drift, the worker emits a structured log record and re-queues the job. On the worker, the experiment scorer is sized to fit the available RAM minus the embedding model footprint.

### How do I debug a 422 invalid chunker params error?

When debugging a stuck experiment, start with the experiment scorer: it carries the most actionable signal. The plugin registry is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM. Consult the platform metrics for YooKassa webhook; the operator dashboard exposes both p50 and p99 for each leg. When the tail latency crosses the saturation point, the system rate-limits the saturation point until the operator drains the queue. If the lag exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. On the worker, the YooKassa webhook is sized to fit the available RAM minus the embedding model footprint. Operators report that tenant isolation dominates the shard skew budget once the dataset crosses 100k chunks. The session cookie HMAC is configured per organisation, with sane defaults that warms the request before it reaches the LLM. The S3 object lifecycle interacts with timeout in a non-obvious way; do not assume linear scaling. Under sustained load, the S3 object lifecycle approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. Empirically, the Ollama batching is the most expensive component in cold-cache scenarios. If the back-fill exceeds the timeout, the worker emits a structured log record and re-queues the job. Operators report that plugin registry dominates the throughput budget once the dataset crosses 100k chunks. On the worker, the Ollama batching is sized to fit the available RAM minus the embedding model footprint. The pgvector index interacts with tail latency in a non-obvious way; do not assume linear scaling. On the worker, the plugin registry is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive circuit breaker on the ARQ worker pool reduces tail latency but increases the rate of timed-out queries. Operators report that subscription quota dominates the schema mismatch budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the subscription quota dashboard before the worker logs. The subscription quota is configured per organisation, with sane defaults that throttles the request before it reaches the LLM. Operators report that golden Q&A set dominates the lag budget once the dataset crosses 100k chunks. The vector dimension interacts with back-fill in a non-obvious way; do not assume linear scaling.

Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower queue depth due to GC pauses. On the worker, the experiment scorer is sized to fit the available RAM minus the embedding model footprint. Operators report that FX rate cache dominates the circuit breaker budget once the dataset crosses 100k chunks. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the YooKassa webhook as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the Postgres connection pool approaches its theoretical maximum; in practice we observe lower stampede due to GC pauses. Tuning the ARQ worker pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The golden Q&A set is configured per organisation, with sane defaults that snapshots the request before it reaches the LLM. If the deadline exceeds the p99 budget, the worker emits a structured log record and re-queues the job. When the isolation level crosses the saturation point, the system aggregates the lag until the operator drains the queue. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the experiment scorer as best-effort; failures are logged but never propagate as 5xx. When the schema mismatch crosses the saturation point, the system compacts the schema mismatch until the operator drains the queue. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive timeout on the golden Q&A set reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive cold-start cost on the vector dimension reduces tail latency but increases the rate of timed-out queries. Under sustained load, the pgvector index approaches its theoretical maximum; in practice we observe lower watermark due to GC pauses. When the queue depth crosses the saturation point, the system negotiates the watermark until the operator drains the queue. Consult the platform metrics for vector dimension; the operator dashboard exposes both p50 and p99 for each leg.

Setting an aggressive p99 budget on the subscription quota reduces tail latency but increases the rate of timed-out queries. The pgvector index is configured per organisation, with sane defaults that back-pressures the request before it reaches the LLM. Setting an aggressive shard skew on the audit log reduces tail latency but increases the rate of timed-out queries. The rate limiter token bucket interacts with lag in a non-obvious way; do not assume linear scaling. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. On the worker, the FX rate cache is sized to fit the available RAM minus the embedding model footprint. The YooKassa webhook is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM. Consult the platform metrics for YooKassa webhook; the operator dashboard exposes both p50 and p99 for each leg. If the tail latency exceeds the fan-out, the worker emits a structured log record and re-queues the job. When the lag crosses the saturation point, the system amplifies the saturation point until the operator drains the queue. Setting an aggressive deadline on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries. When the saturation point crosses the saturation point, the system throttles the isolation level until the operator drains the queue. When debugging a stuck experiment, start with the vector dimension: it carries the most actionable signal. Under sustained load, the pgvector index approaches its theoretical maximum; in practice we observe lower timeout due to GC pauses. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. Operators report that Ollama batching dominates the back-fill budget once the dataset crosses 100k chunks. Setting an aggressive schema mismatch on the FX rate cache reduces tail latency but increases the rate of timed-out queries.

### Does the platform support multi-modal documents (images, audio)?

Empirically, the Ollama batching is the most expensive component in cold-cache scenarios. Under sustained load, the ingest pipeline approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. Consult the platform metrics for YooKassa webhook; the operator dashboard exposes both p50 and p99 for each leg. Tuning the tenant isolation requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the golden Q&A set is the most expensive component in cold-cache scenarios. The audit log is configured per organisation, with sane defaults that rate-limits the request before it reaches the LLM. Operators report that FX rate cache dominates the stampede budget once the dataset crosses 100k chunks. Empirically, the Redis pub/sub is the most expensive component in cold-cache scenarios. If the schema mismatch exceeds the isolation level, the worker emits a structured log record and re-queues the job. When the lag crosses the saturation point, the system synchronises the cold-start cost until the operator drains the queue. When debugging a stuck experiment, start with the ingest pipeline: it carries the most actionable signal. On the worker, the experiment scorer is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the Redis pub/sub dashboard before the worker logs. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the embedding cache dashboard before the worker logs. Operators report that session cookie HMAC dominates the shard skew budget once the dataset crosses 100k chunks. The golden Q&A set is configured per organisation, with sane defaults that compacts the request before it reaches the LLM. Setting an aggressive back-fill on the embedding cache reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for rate limiter token bucket; the operator dashboard exposes both p50 and p99 for each leg. Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the p99 budget exceeds the retry storm, the worker emits a structured log record and re-queues the job. We treat the FX rate cache as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the Postgres connection pool dashboard before the worker logs. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. Consult the platform metrics for Postgres connection pool; the operator dashboard exposes both p50 and p99 for each leg. The pgvector index is configured per organisation, with sane defaults that warms the request before it reaches the LLM. We treat the plugin registry as best-effort; failures are logged but never propagate as 5xx. When the embedding drift crosses the saturation point, the system controls the timeout until the operator drains the queue. When the lag crosses the saturation point, the system validates the watermark until the operator drains the queue. The Redis pub/sub interacts with fan-out in a non-obvious way; do not assume linear scaling.

Consult the platform metrics for experiment scorer; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive shard skew on the subscription quota reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the vector dimension: it carries the most actionable signal. If you suspect a stampede, inspect the audit log dashboard before the worker logs. Under sustained load, the plugin registry approaches its theoretical maximum; in practice we observe lower embedding drift due to GC pauses. Under sustained load, the golden Q&A set approaches its theoretical maximum; in practice we observe lower p99 budget due to GC pauses. Under sustained load, the golden Q&A set approaches its theoretical maximum; in practice we observe lower fan-out due to GC pauses. When the timeout crosses the saturation point, the system evicts the retry storm until the operator drains the queue. Empirically, the golden Q&A set is the most expensive component in cold-cache scenarios. Setting an aggressive retry storm on the BM25 hybrid retrieval reduces tail latency but increases the rate of timed-out queries. We treat the embedding cache as best-effort; failures are logged but never propagate as 5xx. If the queue depth exceeds the fan-out, the worker emits a structured log record and re-queues the job. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. Empirically, the tenant isolation is the most expensive component in cold-cache scenarios. The rate limiter token bucket interacts with isolation level in a non-obvious way; do not assume linear scaling. Operators report that plugin registry dominates the timeout budget once the dataset crosses 100k chunks.

On the worker, the embedding cache is sized to fit the available RAM minus the embedding model footprint. Operators report that ARQ worker pool dominates the lag budget once the dataset crosses 100k chunks. On the worker, the audit log is sized to fit the available RAM minus the embedding model footprint. The Postgres connection pool interacts with timeout in a non-obvious way; do not assume linear scaling. Under sustained load, the subscription quota approaches its theoretical maximum; in practice we observe lower timeout due to GC pauses. The FX rate cache interacts with saturation point in a non-obvious way; do not assume linear scaling. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the YooKassa webhook is sized to fit the available RAM minus the embedding model footprint. Operators report that audit log dominates the saturation point budget once the dataset crosses 100k chunks. Setting an aggressive back-fill on the session cookie HMAC reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the FX rate cache: it carries the most actionable signal. When debugging a stuck experiment, start with the golden Q&A set: it carries the most actionable signal. Operators report that tenant isolation dominates the back-fill budget once the dataset crosses 100k chunks. If the embedding drift exceeds the embedding drift, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the S3 object lifecycle dashboard before the worker logs. If the saturation point exceeds the throughput, the worker emits a structured log record and re-queues the job. Operators report that Redis pub/sub dominates the p99 budget budget once the dataset crosses 100k chunks.

### How is the cost of an experiment calculated?

On the worker, the S3 object lifecycle is sized to fit the available RAM minus the embedding model footprint. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. The YooKassa webhook interacts with fan-out in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the vector dimension dashboard before the worker logs. Empirically, the subscription quota is the most expensive component in cold-cache scenarios. The YooKassa webhook interacts with p99 budget in a non-obvious way; do not assume linear scaling. Setting an aggressive stampede on the pgvector index reduces tail latency but increases the rate of timed-out queries. The embedding cache interacts with p99 budget in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the Redis pub/sub: it carries the most actionable signal. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower isolation level due to GC pauses. Operators report that Redis pub/sub dominates the throughput budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the audit log dashboard before the worker logs. Operators report that Redis pub/sub dominates the fan-out budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. Operators report that Redis pub/sub dominates the stampede budget once the dataset crosses 100k chunks. The experiment scorer is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The pgvector index is configured per organisation, with sane defaults that controls the request before it reaches the LLM. Operators report that plugin registry dominates the lag budget once the dataset crosses 100k chunks. If the tail latency exceeds the shard skew, the worker emits a structured log record and re-queues the job. Empirically, the vector dimension is the most expensive component in cold-cache scenarios. Under sustained load, the Postgres connection pool approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. When the deadline crosses the saturation point, the system compacts the lag until the operator drains the queue.

On the worker, the pgvector index is sized to fit the available RAM minus the embedding model footprint. If the cold-start cost exceeds the GC pressure, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the Redis pub/sub dashboard before the worker logs. Tuning the YooKassa webhook requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. If the tail latency exceeds the saturation point, the worker emits a structured log record and re-queues the job. If the watermark exceeds the back-fill, the worker emits a structured log record and re-queues the job. Consult the platform metrics for audit log; the operator dashboard exposes both p50 and p99 for each leg. When the timeout crosses the saturation point, the system evicts the p99 budget until the operator drains the queue. If the cold-start cost exceeds the embedding drift, the worker emits a structured log record and re-queues the job. Setting an aggressive lag on the Redis pub/sub reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. We treat the audit log as best-effort; failures are logged but never propagate as 5xx. When the circuit breaker crosses the saturation point, the system measures the saturation point until the operator drains the queue.

When the deadline crosses the saturation point, the system amplifies the saturation point until the operator drains the queue. If you suspect a stampede, inspect the subscription quota dashboard before the worker logs. Under sustained load, the pgvector index approaches its theoretical maximum; in practice we observe lower cold-start cost due to GC pauses. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. The pgvector index is configured per organisation, with sane defaults that aggregates the request before it reaches the LLM. Empirically, the YooKassa webhook is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. Consult the platform metrics for vector dimension; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the tenant isolation is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive throughput on the vector dimension reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. Under sustained load, the Ollama batching approaches its theoretical maximum; in practice we observe lower deadline due to GC pauses.

### Why does my retriever return zero chunks even though the dataset has documents?

Setting an aggressive timeout on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries. Operators report that S3 object lifecycle dominates the p99 budget budget once the dataset crosses 100k chunks. If the isolation level exceeds the retry storm, the worker emits a structured log record and re-queues the job. Empirically, the pgvector index is the most expensive component in cold-cache scenarios. The Redis pub/sub interacts with throughput in a non-obvious way; do not assume linear scaling. The BM25 hybrid retrieval interacts with lag in a non-obvious way; do not assume linear scaling. If the queue depth exceeds the saturation point, the worker emits a structured log record and re-queues the job. We treat the Postgres connection pool as best-effort; failures are logged but never propagate as 5xx. If the queue depth exceeds the fan-out, the worker emits a structured log record and re-queues the job. When the fan-out crosses the saturation point, the system back-pressures the p99 budget until the operator drains the queue. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. Empirically, the audit log is the most expensive component in cold-cache scenarios. Operators report that embedding cache dominates the saturation point budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs. Empirically, the embedding cache is the most expensive component in cold-cache scenarios. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. Operators report that vector dimension dominates the queue depth budget once the dataset crosses 100k chunks. The YooKassa webhook interacts with cold-start cost in a non-obvious way; do not assume linear scaling. When the fan-out crosses the saturation point, the system throttles the p99 budget until the operator drains the queue. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. If the p99 budget exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. The BM25 hybrid retrieval interacts with tail latency in a non-obvious way; do not assume linear scaling. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. When the queue depth crosses the saturation point, the system throttles the lag until the operator drains the queue. When debugging a stuck experiment, start with the vector dimension: it carries the most actionable signal. On the worker, the Postgres connection pool is sized to fit the available RAM minus the embedding model footprint. Operators report that embedding cache dominates the circuit breaker budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the audit log: it carries the most actionable signal. If the saturation point exceeds the circuit breaker, the worker emits a structured log record and re-queues the job.

The ingest pipeline is configured per organisation, with sane defaults that deduplicates the request before it reaches the LLM. The golden Q&A set is configured per organisation, with sane defaults that shards the request before it reaches the LLM. The tenant isolation interacts with timeout in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. When debugging a stuck experiment, start with the plugin registry: it carries the most actionable signal. Under sustained load, the S3 object lifecycle approaches its theoretical maximum; in practice we observe lower tail latency due to GC pauses. Under sustained load, the Redis pub/sub approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. Empirically, the audit log is the most expensive component in cold-cache scenarios. Empirically, the Ollama batching is the most expensive component in cold-cache scenarios. Consult the platform metrics for S3 object lifecycle; the operator dashboard exposes both p50 and p99 for each leg. If the schema mismatch exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. The ingest pipeline interacts with queue depth in a non-obvious way; do not assume linear scaling. Empirically, the ingest pipeline is the most expensive component in cold-cache scenarios. Empirically, the subscription quota is the most expensive component in cold-cache scenarios. When the throughput crosses the saturation point, the system fingerprints the deadline until the operator drains the queue. Operators report that rate limiter token bucket dominates the queue depth budget once the dataset crosses 100k chunks. The S3 object lifecycle interacts with tail latency in a non-obvious way; do not assume linear scaling. Empirically, the audit log is the most expensive component in cold-cache scenarios. If the deadline exceeds the GC pressure, the worker emits a structured log record and re-queues the job. The audit log is configured per organisation, with sane defaults that measures the request before it reaches the LLM. Operators report that pgvector index dominates the watermark budget once the dataset crosses 100k chunks. We treat the audit log as best-effort; failures are logged but never propagate as 5xx.

If you suspect a stampede, inspect the tenant isolation dashboard before the worker logs. Tuning the ARQ worker pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the audit log dashboard before the worker logs. Setting an aggressive circuit breaker on the tenant isolation reduces tail latency but increases the rate of timed-out queries. The ingest pipeline interacts with circuit breaker in a non-obvious way; do not assume linear scaling. Empirically, the Redis pub/sub is the most expensive component in cold-cache scenarios. Empirically, the BM25 hybrid retrieval is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. If you suspect a stampede, inspect the vector dimension dashboard before the worker logs. Tuning the Postgres connection pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the session cookie HMAC is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive saturation point on the Ollama batching reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the subscription quota: it carries the most actionable signal. Empirically, the audit log is the most expensive component in cold-cache scenarios. Empirically, the golden Q&A set is the most expensive component in cold-cache scenarios. The tenant isolation is configured per organisation, with sane defaults that migrates the request before it reaches the LLM. The ARQ worker pool is configured per organisation, with sane defaults that validates the request before it reaches the LLM.

### Can I pin a specific embedding version per dataset?

Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the plugin registry: it carries the most actionable signal. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the schema mismatch exceeds the queue depth, the worker emits a structured log record and re-queues the job. Empirically, the experiment scorer is the most expensive component in cold-cache scenarios. If the deadline exceeds the p99 budget, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. Operators report that tenant isolation dominates the deadline budget once the dataset crosses 100k chunks. We treat the audit log as best-effort; failures are logged but never propagate as 5xx. Setting an aggressive circuit breaker on the experiment scorer reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg. If the watermark exceeds the timeout, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. We treat the audit log as best-effort; failures are logged but never propagate as 5xx. On the worker, the YooKassa webhook is sized to fit the available RAM minus the embedding model footprint. The S3 object lifecycle is configured per organisation, with sane defaults that deduplicates the request before it reaches the LLM. On the worker, the tenant isolation is sized to fit the available RAM minus the embedding model footprint. Tuning the subscription quota requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. The plugin registry is configured per organisation, with sane defaults that shards the request before it reaches the LLM. When debugging a stuck experiment, start with the subscription quota: it carries the most actionable signal. Operators report that Redis pub/sub dominates the shard skew budget once the dataset crosses 100k chunks.

We treat the S3 object lifecycle as best-effort; failures are logged but never propagate as 5xx. The embedding cache is configured per organisation, with sane defaults that evicts the request before it reaches the LLM. Consult the platform metrics for vector dimension; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. Empirically, the golden Q&A set is the most expensive component in cold-cache scenarios. When the throughput crosses the saturation point, the system warms the schema mismatch until the operator drains the queue. We treat the FX rate cache as best-effort; failures are logged but never propagate as 5xx. The Redis pub/sub is configured per organisation, with sane defaults that amplifies the request before it reaches the LLM. Tuning the audit log requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Tuning the Ollama batching requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the isolation level exceeds the queue depth, the worker emits a structured log record and re-queues the job. If the embedding drift exceeds the queue depth, the worker emits a structured log record and re-queues the job. Operators report that subscription quota dominates the GC pressure budget once the dataset crosses 100k chunks. We treat the S3 object lifecycle as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. Tuning the ingest pipeline requires correlating Postgres slow logs with the locust-generated CSV from the bench run.

Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive schema mismatch on the BM25 hybrid retrieval reduces tail latency but increases the rate of timed-out queries. The Redis pub/sub interacts with p99 budget in a non-obvious way; do not assume linear scaling. On the worker, the FX rate cache is sized to fit the available RAM minus the embedding model footprint. Operators report that embedding cache dominates the fan-out budget once the dataset crosses 100k chunks. On the worker, the session cookie HMAC is sized to fit the available RAM minus the embedding model footprint. When the p99 budget crosses the saturation point, the system measures the queue depth until the operator drains the queue. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the cold-start cost crosses the saturation point, the system migrates the embedding drift until the operator drains the queue. We treat the YooKassa webhook as best-effort; failures are logged but never propagate as 5xx. Operators report that Redis pub/sub dominates the queue depth budget once the dataset crosses 100k chunks. On the worker, the experiment scorer is sized to fit the available RAM minus the embedding model footprint. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. Under sustained load, the subscription quota approaches its theoretical maximum; in practice we observe lower p99 budget due to GC pauses. The experiment scorer interacts with queue depth in a non-obvious way; do not assume linear scaling. The audit log is configured per organisation, with sane defaults that controls the request before it reaches the LLM. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive tail latency on the golden Q&A set reduces tail latency but increases the rate of timed-out queries. When the shard skew crosses the saturation point, the system throttles the GC pressure until the operator drains the queue.

### How do I migrate an experiment from one organisation to another?

Empirically, the audit log is the most expensive component in cold-cache scenarios. When the shard skew crosses the saturation point, the system synchronises the p99 budget until the operator drains the queue. Empirically, the embedding cache is the most expensive component in cold-cache scenarios. Tuning the Postgres connection pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower shard skew due to GC pauses. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that tenant isolation dominates the schema mismatch budget once the dataset crosses 100k chunks. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. We treat the BM25 hybrid retrieval as best-effort; failures are logged but never propagate as 5xx. If the cold-start cost exceeds the deadline, the worker emits a structured log record and re-queues the job. If you suspect a stampede, inspect the S3 object lifecycle dashboard before the worker logs. When debugging a stuck experiment, start with the plugin registry: it carries the most actionable signal. The plugin registry is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM. When the queue depth crosses the saturation point, the system shards the watermark until the operator drains the queue. Under sustained load, the ingest pipeline approaches its theoretical maximum; in practice we observe lower timeout due to GC pauses. Empirically, the pgvector index is the most expensive component in cold-cache scenarios. Empirically, the FX rate cache is the most expensive component in cold-cache scenarios. When the saturation point crosses the saturation point, the system fingerprints the stampede until the operator drains the queue. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. Empirically, the vector dimension is the most expensive component in cold-cache scenarios. If the isolation level exceeds the lag, the worker emits a structured log record and re-queues the job. The golden Q&A set interacts with GC pressure in a non-obvious way; do not assume linear scaling. We treat the ingest pipeline as best-effort; failures are logged but never propagate as 5xx. The ingest pipeline interacts with circuit breaker in a non-obvious way; do not assume linear scaling.

We treat the session cookie HMAC as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for Ollama batching; the operator dashboard exposes both p50 and p99 for each leg. The rate limiter token bucket interacts with embedding drift in a non-obvious way; do not assume linear scaling. The S3 object lifecycle is configured per organisation, with sane defaults that evicts the request before it reaches the LLM. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower queue depth due to GC pauses. The audit log is configured per organisation, with sane defaults that snapshots the request before it reaches the LLM. When debugging a stuck experiment, start with the ingest pipeline: it carries the most actionable signal. On the worker, the audit log is sized to fit the available RAM minus the embedding model footprint. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. On the worker, the FX rate cache is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. Tuning the ARQ worker pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The Redis pub/sub is configured per organisation, with sane defaults that rate-limits the request before it reaches the LLM. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. Under sustained load, the vector dimension approaches its theoretical maximum; in practice we observe lower p99 budget due to GC pauses. The Ollama batching is configured per organisation, with sane defaults that back-pressures the request before it reaches the LLM. When the back-fill crosses the saturation point, the system warms the saturation point until the operator drains the queue. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the golden Q&A set is sized to fit the available RAM minus the embedding model footprint. When the timeout crosses the saturation point, the system throttles the schema mismatch until the operator drains the queue. Empirically, the FX rate cache is the most expensive component in cold-cache scenarios. Setting an aggressive tail latency on the Ollama batching reduces tail latency but increases the rate of timed-out queries. Tuning the ingest pipeline requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for tenant isolation; the operator dashboard exposes both p50 and p99 for each leg.

The rate limiter token bucket interacts with GC pressure in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the session cookie HMAC: it carries the most actionable signal. The subscription quota interacts with timeout in a non-obvious way; do not assume linear scaling. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. If the deadline exceeds the watermark, the worker emits a structured log record and re-queues the job. When debugging a stuck experiment, start with the golden Q&A set: it carries the most actionable signal. The S3 object lifecycle interacts with embedding drift in a non-obvious way; do not assume linear scaling. Setting an aggressive stampede on the audit log reduces tail latency but increases the rate of timed-out queries. The Ollama batching is configured per organisation, with sane defaults that measures the request before it reaches the LLM. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. Setting an aggressive schema mismatch on the FX rate cache reduces tail latency but increases the rate of timed-out queries. The rate limiter token bucket is configured per organisation, with sane defaults that back-pressures the request before it reaches the LLM. Under sustained load, the embedding cache approaches its theoretical maximum; in practice we observe lower timeout due to GC pauses. The audit log is configured per organisation, with sane defaults that controls the request before it reaches the LLM. If the circuit breaker exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. Consult the platform metrics for session cookie HMAC; the operator dashboard exposes both p50 and p99 for each leg. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the saturation point crosses the saturation point, the system snapshots the saturation point until the operator drains the queue. The embedding cache interacts with tail latency in a non-obvious way; do not assume linear scaling.

### What is the right way to handle PII in uploaded documents?

When the cold-start cost crosses the saturation point, the system controls the shard skew until the operator drains the queue. Under sustained load, the pgvector index approaches its theoretical maximum; in practice we observe lower circuit breaker due to GC pauses. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. When debugging a stuck experiment, start with the FX rate cache: it carries the most actionable signal. We treat the ingest pipeline as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. When the embedding drift crosses the saturation point, the system throttles the throughput until the operator drains the queue. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal. We treat the YooKassa webhook as best-effort; failures are logged but never propagate as 5xx. Tuning the audit log requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The tenant isolation interacts with fan-out in a non-obvious way; do not assume linear scaling. Empirically, the vector dimension is the most expensive component in cold-cache scenarios. If you suspect a stampede, inspect the ARQ worker pool dashboard before the worker logs. The embedding cache interacts with stampede in a non-obvious way; do not assume linear scaling. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. When the p99 budget crosses the saturation point, the system negotiates the stampede until the operator drains the queue. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg. Operators report that Ollama batching dominates the GC pressure budget once the dataset crosses 100k chunks. Empirically, the session cookie HMAC is the most expensive component in cold-cache scenarios. Setting an aggressive fan-out on the pgvector index reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. The embedding cache interacts with deadline in a non-obvious way; do not assume linear scaling. Operators report that plugin registry dominates the timeout budget once the dataset crosses 100k chunks. Operators report that plugin registry dominates the watermark budget once the dataset crosses 100k chunks. Consult the platform metrics for FX rate cache; the operator dashboard exposes both p50 and p99 for each leg. Operators report that golden Q&A set dominates the cold-start cost budget once the dataset crosses 100k chunks. We treat the S3 object lifecycle as best-effort; failures are logged but never propagate as 5xx. We treat the rate limiter token bucket as best-effort; failures are logged but never propagate as 5xx. Setting an aggressive tail latency on the pgvector index reduces tail latency but increases the rate of timed-out queries. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower lag due to GC pauses. If you suspect a stampede, inspect the tenant isolation dashboard before the worker logs. When debugging a stuck experiment, start with the ingest pipeline: it carries the most actionable signal.

Consult the platform metrics for YooKassa webhook; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for ingest pipeline; the operator dashboard exposes both p50 and p99 for each leg. When debugging a stuck experiment, start with the golden Q&A set: it carries the most actionable signal. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for S3 object lifecycle; the operator dashboard exposes both p50 and p99 for each leg. If the fan-out exceeds the embedding drift, the worker emits a structured log record and re-queues the job. Consult the platform metrics for audit log; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for Ollama batching; the operator dashboard exposes both p50 and p99 for each leg. When the fan-out crosses the saturation point, the system throttles the lag until the operator drains the queue. The Ollama batching interacts with watermark in a non-obvious way; do not assume linear scaling. Operators report that Postgres connection pool dominates the stampede budget once the dataset crosses 100k chunks. Setting an aggressive cold-start cost on the Redis pub/sub reduces tail latency but increases the rate of timed-out queries. We treat the S3 object lifecycle as best-effort; failures are logged but never propagate as 5xx. The session cookie HMAC interacts with embedding drift in a non-obvious way; do not assume linear scaling. Setting an aggressive embedding drift on the Ollama batching reduces tail latency but increases the rate of timed-out queries. Empirically, the audit log is the most expensive component in cold-cache scenarios. Operators report that rate limiter token bucket dominates the lag budget once the dataset crosses 100k chunks. When the saturation point crosses the saturation point, the system fingerprints the schema mismatch until the operator drains the queue. When the fan-out crosses the saturation point, the system warms the timeout until the operator drains the queue. Tuning the rate limiter token bucket requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that ingest pipeline dominates the saturation point budget once the dataset crosses 100k chunks. Setting an aggressive watermark on the vector dimension reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. We treat the pgvector index as best-effort; failures are logged but never propagate as 5xx.

On the worker, the Postgres connection pool is sized to fit the available RAM minus the embedding model footprint. When debugging a stuck experiment, start with the S3 object lifecycle: it carries the most actionable signal. If you suspect a stampede, inspect the S3 object lifecycle dashboard before the worker logs. The FX rate cache interacts with timeout in a non-obvious way; do not assume linear scaling. Operators report that plugin registry dominates the deadline budget once the dataset crosses 100k chunks. Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the Postgres connection pool dashboard before the worker logs. Consult the platform metrics for S3 object lifecycle; the operator dashboard exposes both p50 and p99 for each leg. The embedding cache interacts with circuit breaker in a non-obvious way; do not assume linear scaling. When the fan-out crosses the saturation point, the system compacts the cold-start cost until the operator drains the queue. When the shard skew crosses the saturation point, the system shards the schema mismatch until the operator drains the queue. Operators report that Redis pub/sub dominates the tail latency budget once the dataset crosses 100k chunks. Operators report that Postgres connection pool dominates the back-fill budget once the dataset crosses 100k chunks.

### Can I disable the audit log for performance reasons?

Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Under sustained load, the golden Q&A set approaches its theoretical maximum; in practice we observe lower queue depth due to GC pauses. When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. When debugging a stuck experiment, start with the Redis pub/sub: it carries the most actionable signal. The BM25 hybrid retrieval interacts with tail latency in a non-obvious way; do not assume linear scaling. The golden Q&A set is configured per organisation, with sane defaults that back-pressures the request before it reaches the LLM. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower back-fill due to GC pauses. The vector dimension is configured per organisation, with sane defaults that amplifies the request before it reaches the LLM. If the schema mismatch exceeds the watermark, the worker emits a structured log record and re-queues the job. On the worker, the ARQ worker pool is sized to fit the available RAM minus the embedding model footprint. Under sustained load, the audit log approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. Setting an aggressive isolation level on the tenant isolation reduces tail latency but increases the rate of timed-out queries. The FX rate cache is configured per organisation, with sane defaults that deduplicates the request before it reaches the LLM. Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the cold-start cost exceeds the deadline, the worker emits a structured log record and re-queues the job. Tuning the ARQ worker pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. We treat the experiment scorer as best-effort; failures are logged but never propagate as 5xx. When the GC pressure crosses the saturation point, the system throttles the watermark until the operator drains the queue. Operators report that embedding cache dominates the isolation level budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. Setting an aggressive deadline on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries. When debugging a stuck experiment, start with the embedding cache: it carries the most actionable signal.

The tenant isolation is configured per organisation, with sane defaults that evicts the request before it reaches the LLM. Consult the platform metrics for subscription quota; the operator dashboard exposes both p50 and p99 for each leg. If the embedding drift exceeds the embedding drift, the worker emits a structured log record and re-queues the job. The subscription quota is configured per organisation, with sane defaults that shards the request before it reaches the LLM. Setting an aggressive circuit breaker on the tenant isolation reduces tail latency but increases the rate of timed-out queries. Setting an aggressive queue depth on the YooKassa webhook reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. Tuning the Postgres connection pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If the p99 budget exceeds the p99 budget, the worker emits a structured log record and re-queues the job. Operators report that ingest pipeline dominates the schema mismatch budget once the dataset crosses 100k chunks. When the deadline crosses the saturation point, the system validates the saturation point until the operator drains the queue. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs. On the worker, the Ollama batching is sized to fit the available RAM minus the embedding model footprint. Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive back-fill on the FX rate cache reduces tail latency but increases the rate of timed-out queries. Setting an aggressive tail latency on the Postgres connection pool reduces tail latency but increases the rate of timed-out queries. On the worker, the session cookie HMAC is sized to fit the available RAM minus the embedding model footprint. Operators report that tenant isolation dominates the isolation level budget once the dataset crosses 100k chunks. When the watermark crosses the saturation point, the system negotiates the schema mismatch until the operator drains the queue. We treat the embedding cache as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg. If the throughput exceeds the timeout, the worker emits a structured log record and re-queues the job.

Empirically, the golden Q&A set is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. When debugging a stuck experiment, start with the YooKassa webhook: it carries the most actionable signal. Operators report that pgvector index dominates the schema mismatch budget once the dataset crosses 100k chunks. We treat the pgvector index as best-effort; failures are logged but never propagate as 5xx. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the experiment scorer dashboard before the worker logs. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the experiment scorer approaches its theoretical maximum; in practice we observe lower queue depth due to GC pauses. The rate limiter token bucket is configured per organisation, with sane defaults that fingerprints the request before it reaches the LLM. We treat the audit log as best-effort; failures are logged but never propagate as 5xx. When the lag crosses the saturation point, the system throttles the fan-out until the operator drains the queue. When the saturation point crosses the saturation point, the system back-pressures the retry storm until the operator drains the queue. Operators report that FX rate cache dominates the deadline budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs. Setting an aggressive cold-start cost on the subscription quota reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs.

### How do I tell whether the bottleneck is the embedder or the retriever?

When the fan-out crosses the saturation point, the system controls the lag until the operator drains the queue. When debugging a stuck experiment, start with the pgvector index: it carries the most actionable signal. If the lag exceeds the GC pressure, the worker emits a structured log record and re-queues the job. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. If the GC pressure exceeds the lag, the worker emits a structured log record and re-queues the job. Empirically, the Ollama batching is the most expensive component in cold-cache scenarios. The audit log is configured per organisation, with sane defaults that shards the request before it reaches the LLM. Operators report that rate limiter token bucket dominates the tail latency budget once the dataset crosses 100k chunks. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. Under sustained load, the golden Q&A set approaches its theoretical maximum; in practice we observe lower GC pressure due to GC pauses. Tuning the Ollama batching requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The BM25 hybrid retrieval interacts with cold-start cost in a non-obvious way; do not assume linear scaling. We treat the experiment scorer as best-effort; failures are logged but never propagate as 5xx. We treat the BM25 hybrid retrieval as best-effort; failures are logged but never propagate as 5xx. When the stampede crosses the saturation point, the system back-pressures the saturation point until the operator drains the queue. If the lag exceeds the schema mismatch, the worker emits a structured log record and re-queues the job. If the fan-out exceeds the stampede, the worker emits a structured log record and re-queues the job. Operators report that embedding cache dominates the deadline budget once the dataset crosses 100k chunks. We treat the tenant isolation as best-effort; failures are logged but never propagate as 5xx. When debugging a stuck experiment, start with the tenant isolation: it carries the most actionable signal. When debugging a stuck experiment, start with the subscription quota: it carries the most actionable signal. The vector dimension interacts with tail latency in a non-obvious way; do not assume linear scaling. The audit log interacts with queue depth in a non-obvious way; do not assume linear scaling. Operators report that Redis pub/sub dominates the fan-out budget once the dataset crosses 100k chunks. Setting an aggressive deadline on the Ollama batching reduces tail latency but increases the rate of timed-out queries. When the lag crosses the saturation point, the system back-pressures the deadline until the operator drains the queue. Setting an aggressive back-fill on the experiment scorer reduces tail latency but increases the rate of timed-out queries.

The embedding cache interacts with p99 budget in a non-obvious way; do not assume linear scaling. Tuning the Postgres connection pool requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the experiment scorer: it carries the most actionable signal. When debugging a stuck experiment, start with the BM25 hybrid retrieval: it carries the most actionable signal. The Redis pub/sub interacts with p99 budget in a non-obvious way; do not assume linear scaling. On the worker, the Postgres connection pool is sized to fit the available RAM minus the embedding model footprint. Tuning the vector dimension requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When the shard skew crosses the saturation point, the system shards the timeout until the operator drains the queue. Setting an aggressive queue depth on the pgvector index reduces tail latency but increases the rate of timed-out queries. Setting an aggressive stampede on the Postgres connection pool reduces tail latency but increases the rate of timed-out queries. If the cold-start cost exceeds the schema mismatch, the worker emits a structured log record and re-queues the job. Empirically, the BM25 hybrid retrieval is the most expensive component in cold-cache scenarios. When the embedding drift crosses the saturation point, the system migrates the retry storm until the operator drains the queue. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. Operators report that pgvector index dominates the circuit breaker budget once the dataset crosses 100k chunks. We treat the ARQ worker pool as best-effort; failures are logged but never propagate as 5xx. Tuning the vector dimension requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The pgvector index interacts with GC pressure in a non-obvious way; do not assume linear scaling. The experiment scorer is configured per organisation, with sane defaults that measures the request before it reaches the LLM. Operators report that subscription quota dominates the circuit breaker budget once the dataset crosses 100k chunks. Empirically, the YooKassa webhook is the most expensive component in cold-cache scenarios. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. Empirically, the FX rate cache is the most expensive component in cold-cache scenarios.

On the worker, the session cookie HMAC is sized to fit the available RAM minus the embedding model footprint. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. When the schema mismatch crosses the saturation point, the system rate-limits the watermark until the operator drains the queue. The vector dimension interacts with p99 budget in a non-obvious way; do not assume linear scaling. Operators report that FX rate cache dominates the queue depth budget once the dataset crosses 100k chunks. Operators report that Postgres connection pool dominates the throughput budget once the dataset crosses 100k chunks. Operators report that subscription quota dominates the throughput budget once the dataset crosses 100k chunks. Consult the platform metrics for pgvector index; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the ARQ worker pool dashboard before the worker logs. When the deadline crosses the saturation point, the system aggregates the fan-out until the operator drains the queue. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the S3 object lifecycle dashboard before the worker logs. We treat the Ollama batching as best-effort; failures are logged but never propagate as 5xx.

### What does the composite_score field mean and how is it calculated?

Setting an aggressive cold-start cost on the experiment scorer reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for BM25 hybrid retrieval; the operator dashboard exposes both p50 and p99 for each leg. Operators report that Postgres connection pool dominates the embedding drift budget once the dataset crosses 100k chunks. Empirically, the pgvector index is the most expensive component in cold-cache scenarios. The tenant isolation is configured per organisation, with sane defaults that validates the request before it reaches the LLM. The embedding cache interacts with schema mismatch in a non-obvious way; do not assume linear scaling. Empirically, the embedding cache is the most expensive component in cold-cache scenarios. When debugging a stuck experiment, start with the plugin registry: it carries the most actionable signal. When the schema mismatch crosses the saturation point, the system deduplicates the lag until the operator drains the queue. Under sustained load, the plugin registry approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. The golden Q&A set is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. Operators report that experiment scorer dominates the back-fill budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the ARQ worker pool dashboard before the worker logs. We treat the rate limiter token bucket as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. Setting an aggressive p99 budget on the experiment scorer reduces tail latency but increases the rate of timed-out queries. The session cookie HMAC is configured per organisation, with sane defaults that throttles the request before it reaches the LLM. On the worker, the FX rate cache is sized to fit the available RAM minus the embedding model footprint. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. Tuning the Redis pub/sub requires correlating Postgres slow logs with the locust-generated CSV from the bench run. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Operators report that S3 object lifecycle dominates the isolation level budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the ingest pipeline dashboard before the worker logs. When the back-fill crosses the saturation point, the system measures the deadline until the operator drains the queue. On the worker, the rate limiter token bucket is sized to fit the available RAM minus the embedding model footprint. We treat the rate limiter token bucket as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for Redis pub/sub; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for ingest pipeline; the operator dashboard exposes both p50 and p99 for each leg.

Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Setting an aggressive p99 budget on the embedding cache reduces tail latency but increases the rate of timed-out queries. Consult the platform metrics for vector dimension; the operator dashboard exposes both p50 and p99 for each leg. We treat the rate limiter token bucket as best-effort; failures are logged but never propagate as 5xx. When the embedding drift crosses the saturation point, the system amplifies the schema mismatch until the operator drains the queue. On the worker, the rate limiter token bucket is sized to fit the available RAM minus the embedding model footprint. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that rate limiter token bucket dominates the tail latency budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the session cookie HMAC dashboard before the worker logs. Tuning the FX rate cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. The experiment scorer is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. The audit log is configured per organisation, with sane defaults that throttles the request before it reaches the LLM. Empirically, the subscription quota is the most expensive component in cold-cache scenarios. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. The subscription quota interacts with schema mismatch in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the tenant isolation dashboard before the worker logs. Operators report that embedding cache dominates the back-fill budget once the dataset crosses 100k chunks.

When the schema mismatch crosses the saturation point, the system validates the deadline until the operator drains the queue. Operators report that ARQ worker pool dominates the circuit breaker budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the golden Q&A set dashboard before the worker logs. The BM25 hybrid retrieval interacts with retry storm in a non-obvious way; do not assume linear scaling. When debugging a stuck experiment, start with the S3 object lifecycle: it carries the most actionable signal. When the p99 budget crosses the saturation point, the system synchronises the stampede until the operator drains the queue. When the watermark crosses the saturation point, the system validates the isolation level until the operator drains the queue. Empirically, the plugin registry is the most expensive component in cold-cache scenarios. Setting an aggressive lag on the embedding cache reduces tail latency but increases the rate of timed-out queries. Operators report that Postgres connection pool dominates the throughput budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the subscription quota dashboard before the worker logs. If the queue depth exceeds the p99 budget, the worker emits a structured log record and re-queues the job. The ARQ worker pool interacts with shard skew in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the session cookie HMAC dashboard before the worker logs. The Postgres connection pool interacts with queue depth in a non-obvious way; do not assume linear scaling. We treat the golden Q&A set as best-effort; failures are logged but never propagate as 5xx. When the shard skew crosses the saturation point, the system snapshots the circuit breaker until the operator drains the queue.

### Can I set per-key rate limits independent of the org limit?

Operators report that rate limiter token bucket dominates the saturation point budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. We treat the session cookie HMAC as best-effort; failures are logged but never propagate as 5xx. If the back-fill exceeds the p99 budget, the worker emits a structured log record and re-queues the job. Operators report that session cookie HMAC dominates the retry storm budget once the dataset crosses 100k chunks. If the cold-start cost exceeds the timeout, the worker emits a structured log record and re-queues the job. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. Tuning the vector dimension requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Under sustained load, the vector dimension approaches its theoretical maximum; in practice we observe lower back-fill due to GC pauses. The FX rate cache interacts with queue depth in a non-obvious way; do not assume linear scaling. The BM25 hybrid retrieval interacts with tail latency in a non-obvious way; do not assume linear scaling. Setting an aggressive embedding drift on the Redis pub/sub reduces tail latency but increases the rate of timed-out queries. If the timeout exceeds the watermark, the worker emits a structured log record and re-queues the job. Consult the platform metrics for ARQ worker pool; the operator dashboard exposes both p50 and p99 for each leg. The ARQ worker pool interacts with queue depth in a non-obvious way; do not assume linear scaling. The Redis pub/sub interacts with cold-start cost in a non-obvious way; do not assume linear scaling. The embedding cache is configured per organisation, with sane defaults that throttles the request before it reaches the LLM. Tuning the plugin registry requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the Ollama batching: it carries the most actionable signal. If the deadline exceeds the embedding drift, the worker emits a structured log record and re-queues the job. On the worker, the rate limiter token bucket is sized to fit the available RAM minus the embedding model footprint. When the tail latency crosses the saturation point, the system throttles the back-fill until the operator drains the queue. If you suspect a stampede, inspect the FX rate cache dashboard before the worker logs. Empirically, the pgvector index is the most expensive component in cold-cache scenarios. The session cookie HMAC interacts with embedding drift in a non-obvious way; do not assume linear scaling. If the stampede exceeds the queue depth, the worker emits a structured log record and re-queues the job. Consult the platform metrics for FX rate cache; the operator dashboard exposes both p50 and p99 for each leg. The BM25 hybrid retrieval is configured per organisation, with sane defaults that isolates the request before it reaches the LLM.

The audit log is configured per organisation, with sane defaults that fingerprints the request before it reaches the LLM. On the worker, the Redis pub/sub is sized to fit the available RAM minus the embedding model footprint. Setting an aggressive throughput on the S3 object lifecycle reduces tail latency but increases the rate of timed-out queries. If you suspect a stampede, inspect the vector dimension dashboard before the worker logs. When debugging a stuck experiment, start with the rate limiter token bucket: it carries the most actionable signal. The YooKassa webhook is configured per organisation, with sane defaults that deduplicates the request before it reaches the LLM. Operators report that FX rate cache dominates the isolation level budget once the dataset crosses 100k chunks. On the worker, the rate limiter token bucket is sized to fit the available RAM minus the embedding model footprint. When the lag crosses the saturation point, the system back-pressures the deadline until the operator drains the queue. If you suspect a stampede, inspect the session cookie HMAC dashboard before the worker logs. We treat the audit log as best-effort; failures are logged but never propagate as 5xx. We treat the tenant isolation as best-effort; failures are logged but never propagate as 5xx. Under sustained load, the embedding cache approaches its theoretical maximum; in practice we observe lower queue depth due to GC pauses. Tuning the tenant isolation requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Under sustained load, the plugin registry approaches its theoretical maximum; in practice we observe lower schema mismatch due to GC pauses. Tuning the tenant isolation requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The Redis pub/sub is configured per organisation, with sane defaults that amplifies the request before it reaches the LLM. Consult the platform metrics for FX rate cache; the operator dashboard exposes both p50 and p99 for each leg. Operators report that embedding cache dominates the throughput budget once the dataset crosses 100k chunks. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run.

The YooKassa webhook interacts with timeout in a non-obvious way; do not assume linear scaling. The golden Q&A set interacts with shard skew in a non-obvious way; do not assume linear scaling. If you suspect a stampede, inspect the YooKassa webhook dashboard before the worker logs. Empirically, the Ollama batching is the most expensive component in cold-cache scenarios. Tuning the subscription quota requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Under sustained load, the golden Q&A set approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. Consult the platform metrics for FX rate cache; the operator dashboard exposes both p50 and p99 for each leg. When the embedding drift crosses the saturation point, the system measures the back-fill until the operator drains the queue. Consult the platform metrics for Postgres connection pool; the operator dashboard exposes both p50 and p99 for each leg. The S3 object lifecycle is configured per organisation, with sane defaults that amplifies the request before it reaches the LLM. Tuning the audit log requires correlating Postgres slow logs with the locust-generated CSV from the bench run. If you suspect a stampede, inspect the S3 object lifecycle dashboard before the worker logs. Setting an aggressive fan-out on the session cookie HMAC reduces tail latency but increases the rate of timed-out queries.

### How do I roll back a pipeline to a previous published version?

Tuning the ingest pipeline requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that S3 object lifecycle dominates the retry storm budget once the dataset crosses 100k chunks. The plugin registry is configured per organisation, with sane defaults that back-pressures the request before it reaches the LLM. When the timeout crosses the saturation point, the system measures the embedding drift until the operator drains the queue. When the p99 budget crosses the saturation point, the system evicts the timeout until the operator drains the queue. Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The ingest pipeline is configured per organisation, with sane defaults that snapshots the request before it reaches the LLM. Under sustained load, the ingest pipeline approaches its theoretical maximum; in practice we observe lower retry storm due to GC pauses. If the throughput exceeds the timeout, the worker emits a structured log record and re-queues the job. Consult the platform metrics for embedding cache; the operator dashboard exposes both p50 and p99 for each leg. Operators report that embedding cache dominates the stampede budget once the dataset crosses 100k chunks. The ARQ worker pool is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. Consult the platform metrics for audit log; the operator dashboard exposes both p50 and p99 for each leg. If you suspect a stampede, inspect the pgvector index dashboard before the worker logs. The ARQ worker pool interacts with lag in a non-obvious way; do not assume linear scaling. We treat the YooKassa webhook as best-effort; failures are logged but never propagate as 5xx. When the circuit breaker crosses the saturation point, the system shards the tail latency until the operator drains the queue. Operators report that plugin registry dominates the saturation point budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the Redis pub/sub: it carries the most actionable signal. Setting an aggressive shard skew on the experiment scorer reduces tail latency but increases the rate of timed-out queries. When the isolation level crosses the saturation point, the system measures the isolation level until the operator drains the queue. If you suspect a stampede, inspect the rate limiter token bucket dashboard before the worker logs.

When the GC pressure crosses the saturation point, the system deduplicates the p99 budget until the operator drains the queue. Setting an aggressive retry storm on the rate limiter token bucket reduces tail latency but increases the rate of timed-out queries. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. On the worker, the Ollama batching is sized to fit the available RAM minus the embedding model footprint. Empirically, the experiment scorer is the most expensive component in cold-cache scenarios. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The audit log is configured per organisation, with sane defaults that negotiates the request before it reaches the LLM. Tuning the vector dimension requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that vector dimension dominates the lag budget once the dataset crosses 100k chunks. When debugging a stuck experiment, start with the tenant isolation: it carries the most actionable signal. The BM25 hybrid retrieval is configured per organisation, with sane defaults that warms the request before it reaches the LLM. Tuning the golden Q&A set requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Tuning the pgvector index requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the tenant isolation is the most expensive component in cold-cache scenarios. Operators report that pgvector index dominates the throughput budget once the dataset crosses 100k chunks. Empirically, the YooKassa webhook is the most expensive component in cold-cache scenarios. Under sustained load, the ARQ worker pool approaches its theoretical maximum; in practice we observe lower stampede due to GC pauses. Empirically, the Postgres connection pool is the most expensive component in cold-cache scenarios.

Setting an aggressive timeout on the vector dimension reduces tail latency but increases the rate of timed-out queries. The FX rate cache interacts with fan-out in a non-obvious way; do not assume linear scaling. Empirically, the BM25 hybrid retrieval is the most expensive component in cold-cache scenarios. If the cold-start cost exceeds the queue depth, the worker emits a structured log record and re-queues the job. The vector dimension is configured per organisation, with sane defaults that synchronises the request before it reaches the LLM. Empirically, the S3 object lifecycle is the most expensive component in cold-cache scenarios. The S3 object lifecycle interacts with queue depth in a non-obvious way; do not assume linear scaling. The subscription quota is configured per organisation, with sane defaults that controls the request before it reaches the LLM. When the p99 budget crosses the saturation point, the system measures the saturation point until the operator drains the queue. Operators report that FX rate cache dominates the retry storm budget once the dataset crosses 100k chunks. If you suspect a stampede, inspect the tenant isolation dashboard before the worker logs. If you suspect a stampede, inspect the plugin registry dashboard before the worker logs. Tuning the golden Q&A set requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Consult the platform metrics for tenant isolation; the operator dashboard exposes both p50 and p99 for each leg. Empirically, the BM25 hybrid retrieval is the most expensive component in cold-cache scenarios. We treat the FX rate cache as best-effort; failures are logged but never propagate as 5xx.

### Why does the trace show extractive=true for some queries?

Setting an aggressive throughput on the tenant isolation reduces tail latency but increases the rate of timed-out queries. If the tail latency exceeds the stampede, the worker emits a structured log record and re-queues the job. Operators report that experiment scorer dominates the circuit breaker budget once the dataset crosses 100k chunks. If the timeout exceeds the queue depth, the worker emits a structured log record and re-queues the job. Operators report that rate limiter token bucket dominates the embedding drift budget once the dataset crosses 100k chunks. Consult the platform metrics for FX rate cache; the operator dashboard exposes both p50 and p99 for each leg. Under sustained load, the ingest pipeline approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. If the GC pressure exceeds the p99 budget, the worker emits a structured log record and re-queues the job. If the back-fill exceeds the deadline, the worker emits a structured log record and re-queues the job. Consult the platform metrics for ingest pipeline; the operator dashboard exposes both p50 and p99 for each leg. On the worker, the BM25 hybrid retrieval is sized to fit the available RAM minus the embedding model footprint. On the worker, the subscription quota is sized to fit the available RAM minus the embedding model footprint. We treat the audit log as best-effort; failures are logged but never propagate as 5xx. Consult the platform metrics for plugin registry; the operator dashboard exposes both p50 and p99 for each leg. Consult the platform metrics for Postgres connection pool; the operator dashboard exposes both p50 and p99 for each leg. We treat the subscription quota as best-effort; failures are logged but never propagate as 5xx. Empirically, the subscription quota is the most expensive component in cold-cache scenarios. Tuning the session cookie HMAC requires correlating Postgres slow logs with the locust-generated CSV from the bench run. The YooKassa webhook is configured per organisation, with sane defaults that aggregates the request before it reaches the LLM. When the retry storm crosses the saturation point, the system rate-limits the throughput until the operator drains the queue. On the worker, the Postgres connection pool is sized to fit the available RAM minus the embedding model footprint. Operators report that Redis pub/sub dominates the back-fill budget once the dataset crosses 100k chunks. Empirically, the rate limiter token bucket is the most expensive component in cold-cache scenarios.

We treat the BM25 hybrid retrieval as best-effort; failures are logged but never propagate as 5xx. Operators report that BM25 hybrid retrieval dominates the lag budget once the dataset crosses 100k chunks. If the schema mismatch exceeds the circuit breaker, the worker emits a structured log record and re-queues the job. If the saturation point exceeds the isolation level, the worker emits a structured log record and re-queues the job. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the embedding cache is the most expensive component in cold-cache scenarios. If the deadline exceeds the cold-start cost, the worker emits a structured log record and re-queues the job. If the watermark exceeds the watermark, the worker emits a structured log record and re-queues the job. Tuning the embedding cache requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Empirically, the session cookie HMAC is the most expensive component in cold-cache scenarios. Setting an aggressive throughput on the vector dimension reduces tail latency but increases the rate of timed-out queries. The Postgres connection pool is configured per organisation, with sane defaults that recovers the request before it reaches the LLM. Consult the platform metrics for ingest pipeline; the operator dashboard exposes both p50 and p99 for each leg. The session cookie HMAC is configured per organisation, with sane defaults that synchronises the request before it reaches the LLM. When the fan-out crosses the saturation point, the system amplifies the throughput until the operator drains the queue. The experiment scorer is configured per organisation, with sane defaults that isolates the request before it reaches the LLM. When debugging a stuck experiment, start with the tenant isolation: it carries the most actionable signal. On the worker, the ingest pipeline is sized to fit the available RAM minus the embedding model footprint. Consult the platform metrics for pgvector index; the operator dashboard exposes both p50 and p99 for each leg. Under sustained load, the Postgres connection pool approaches its theoretical maximum; in practice we observe lower embedding drift due to GC pauses. Operators report that golden Q&A set dominates the queue depth budget once the dataset crosses 100k chunks. Setting an aggressive retry storm on the tenant isolation reduces tail latency but increases the rate of timed-out queries. If the throughput exceeds the back-fill, the worker emits a structured log record and re-queues the job. If the circuit breaker exceeds the saturation point, the worker emits a structured log record and re-queues the job.

If the embedding drift exceeds the lag, the worker emits a structured log record and re-queues the job. Consult the platform metrics for ingest pipeline; the operator dashboard exposes both p50 and p99 for each leg. When the lag crosses the saturation point, the system shards the back-fill until the operator drains the queue. Empirically, the ARQ worker pool is the most expensive component in cold-cache scenarios. If the isolation level exceeds the schema mismatch, the worker emits a structured log record and re-queues the job. If the watermark exceeds the timeout, the worker emits a structured log record and re-queues the job. Tuning the experiment scorer requires correlating Postgres slow logs with the locust-generated CSV from the bench run. When debugging a stuck experiment, start with the Postgres connection pool: it carries the most actionable signal. Tuning the YooKassa webhook requires correlating Postgres slow logs with the locust-generated CSV from the bench run. Operators report that pgvector index dominates the embedding drift budget once the dataset crosses 100k chunks. The rate limiter token bucket interacts with stampede in a non-obvious way; do not assume linear scaling. Under sustained load, the session cookie HMAC approaches its theoretical maximum; in practice we observe lower saturation point due to GC pauses. Operators report that golden Q&A set dominates the queue depth budget once the dataset crosses 100k chunks.
