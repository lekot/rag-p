apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-p.fullname" . }}-web
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "rag-p.componentLabels" (dict "root" . "component" "web") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rag-p.componentSelectorLabels" (dict "root" . "component" "web") | nindent 4 }}
